---
title: Recursive Harness Self-Improvement
title_zh: 递归框架自我改进
authors: "Hyunin Lee, Jinglue Xu, Jeffrey Seely, Donghyun Lee, Matei Zaharia, Yujin Tang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15524v1"
tags: ["query:agent-lsh"]
score: 10.0
evidence: 递归驾驭自我改进，迭代优化代理循环
tldr: "在模型与框架协同进化中，框架不仅是推理脚手架，更是数据生成器。为降低频繁更新框架的高昂成本，提出Recursive Harness Self-Improvement (RHI)方法，将框架表示为智能体循环的提示层级规范，利用历史修订的成对反馈迭代自改进。在30个合成机器学习任务上，RHI显著提升低推理成本智能体的性能上限，甚至超越高推理成本设置，同时降低60%推理开销。改进主要源于任务特定上下文管理优化而非推理链延长，为持续学习提供了实用方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15524v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 5000, \"height\": 1336}, {\"url\": \"assets/figures/arxiv/2607.15524v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2607.15524v1/fig-003.webp\", \"caption\": \"\", \"page\": 9, \"index\": 3, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2607.15524v1/fig-004.webp\", \"caption\": \"\", \"page\": 32, \"index\": 4, \"width\": 2040, \"height\": 1200}, {\"url\": \"assets/figures/arxiv/2607.15524v1/fig-005.webp\", \"caption\": \"\", \"page\": 32, \"index\": 5, \"width\": 2040, \"height\": 1200}]"
motivation: 优化用户构建的框架以生成高质量执行轨迹，同时避免高昂的人工更新成本。
method: RHI将框架表示为智能体循环的提示规范，并基于自身修订历史的成对反馈进行迭代精炼。
result: "在30个跨领域任务中，RHI使低推理成本智能体性能超越最高推理成本设置，并降低60%推理成本。"
conclusion: RHI通过改进智能体间信息流提升上下文管理效率，是模型-框架协同进化中持续学习的实用算法。
---

## 摘要
在模型-框架共同演化中，框架不仅是推理时的脚手架，更是数据生成组件，其执行轨迹能够塑造未来的基础模型。这激发了框架在环学习：优化框架以同时提升即时代理性能以及用于未来模型训练的轨迹质量。然而，持续更新提供商构建的脚手架代价高昂且劳动密集。因此，我们研究是否可以通过特定任务方式优化用户构建的框架，在执行轨迹质量提升的同时保持计算轻量且仅需少量更新迭代。为此，我们引入递归框架自我改进（RHI），它将框架表示为代理循环的提示级规范，并利用其自身修订历史中的成对反馈迭代优化。在涵盖量化金融、机器人学和制药的30个合成机器学习研究任务中，少量RHI迭代足以显著提高低推理努力代理的性能上限，超过相应的最大推理努力设置，同时将推理成本降低多达60%。我们表明，这些收益主要来自通过更有效的代理间信息流改进的任务特定上下文管理，而非更长的推理轨迹。最后，我们将这一行为形式化为RHI隐式优化目标的信息论假设，表明RHI是在模型-框架共同演化范式中进行持续学习的实用算法。

## Abstract
Under model--harness co-evolution, harnesses are not merely inference-time scaffolds but data-generating components whose execution traces can shape future foundation models. This motivates harness-in-the-loop learning: optimizing harnesses for both immediate agent performance and the quality of traces used for future model training. However, continually updating provider-built scaffolds is costly and labor-intensive. We therefore investigate whether optimizing user-constructed harnesses in a task-specific manner can improve execution-trace quality while remaining computationally lightweight and requiring only a few update iterations. To this end, we introduce Recursive Harness Self-Improvement (RHI), which represents the harness as a prompt-level specification of the agent loop and iteratively refines it using pairwise feedback over its own revision history. Across 30 synthetic machine-learning research tasks spanning quantitative finance, robotics, and pharmacy, a few RHI iterations suffice to substantially raise the performance ceiling of low-reasoning-effort agents, exceeding the corresponding maximum-reasoning-effort setting while reducing inference cost by up to 60%. We show that these gains arise primarily from improved task-specific context management through more effective inter-agent information flow rather than longer reasoning traces. Finally, we formalize this behavior as an information-theoretic hypothesis for RHI's implicit optimization objective, suggesting RHI as a practical algorithm for continual learning within the paradigm of model--harness co-evolution.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义（研究动机和背景）

- 在模型与框架（harness）协同演化（model–harness co-evolution）的视角下，框架不仅是在推理时组织模型行为的脚手架，更是生成执行轨迹的数据生成组件，这些轨迹可用于训练未来的基础模型。
- 因此，优化框架的目标是双重的：提升即时代理（agent）性能，同时提高生成轨迹的质量，以服务于未来模型的持续学习。
- 然而，由提供商构建的通用框架需要覆盖广泛用户和任务，难以频繁更新；用户构建的任务特定框架则可通过轻量级优化来提升轨迹质量，但现有方法（如群体搜索）成本高昂且需要大量迭代。
- 核心问题：**能否设计一种计算轻量、仅需少数迭代的任务特定框架优化方法，以显著提升代理性能，甚至超越更耗时的推理缩放（test-time scaling）设置？**

### 论文提出的方法论：核心思想、关键技术细节

- **方法名称**：递归框架自我改进（Recursive Harness Self-Improvement, RHI）
- **核心思想**：将框架视为一个提示级别的规范，定义代理循环（agent loop）中的角色、指令、信息交换协议（contract）、工作流步骤（hop）等。RHI通过比较当前框架生成的输出与其前一次迭代的输出（成对偏好），累积反馈历史，然后利用 LLM 作为框架优化器，基于历史偏好迭代地修改框架文本。
- **关键技术细节**：
  - **框架表示**：框架由“代理设计”（角色和指令）和“代理工作流”（契约和跳转）组成。契约（contract）指定子代理与协调器之间传递的信息接口，跳转（hop）定义交互结构的控制流。
  - **轻量级优化策略**：RHI 不使用群体搜索，而是采用轨迹局部松弛：每次迭代仅将当前框架与上一个框架（而非完整参考分布）进行成对比较，仅需一次代理执行和一次成对评估（cost Θ(1)），远低于群体方法的 Θ(m²)。
  - **历史累积**：成对反馈被累积成自我比较历史 Dₓ⁽ⁱ⁾，作为动量信号指导后续更新，提高在少量迭代下的鲁棒性。
  - **更新规则**：𝐻ₓ⁽ⁱ⁺¹⁾ = L_harness(𝐻ₓ⁽ⁱ⁾, Dₓ⁽ⁱ⁾)，其中 L_harness 是 LLM 优化器，不直接看到评估提示 x_eval，仅通过反馈历史间接对齐。
  - **算法流程**：初始化框架，代理生成输出；评估当前输出与上一次输出的偏好；更新历史；优化器根据历史更新框架；重复直到改善率低于阈值。
- **公式/算法**：文中给出了局部偏好目标函数（公式3）和完整算法伪代码（Algorithm 1），重点在于轻量、局部比较和递归自改进。

### 实验设计：数据集/场景、benchmark、对比方法

- **任务基准**：30 个合成的、开放式机器学习研究任务，涵盖三个领域各 10 个：量化金融（quantitative finance）、机器人学（robotics）、制药（pharmacy）。任务由 LLM 将行业招聘信息转化为研究提示，要求生成完整代码仓库（包括 research_report.md、可视化图、metrics.json、index.json 等）。
- **评估协议**：使用 LLM-as-a-judge 进行成对比较（pairwise comparison），评估标准包括交付物覆盖、数值与实证严谨性、可复现性、展示质量、工程质量、任务对齐。使用了两种评估器配置（gpt-5.5-max 和 opus-4.7/4.8-xhigh）及三个随机种子。
- **对比方法**：
  - 基础模型：Claude Sonnet 4.6、Opus 4.7、Opus 4.8，均使用 high 推理努力。
  - 测试时缩放基线（test-time scaling baselines）：xhigh、max、ultracode 推理努力设置（无 RHI 框架优化）。
  - 消融实验：比较单代理默认、内置多代理、多代理+初始框架、单代理+联合角色等设置。还分析了框架组件（角色、指令、契约、跳转）的演化。

### 资源与算力

- **未明确说明**：论文没有提供使用的 GPU 型号、数量、训练时长等硬件资源信息。仅提到使用 Claude 系列模型（Sonnet 4.6, Opus 4.7, Opus 4.8）的 API 调用，并通过成本归一化比较推理开销。RHI 本身是轻量级的，每次迭代仅需一次代理执行和一次成对评估，因此资源需求远低于群体搜索方法。

### 实验数量与充分性

- **主要实验**：在 30 个任务上比较了 RHI 与三种测试时缩放基线（xhigh/max/ultracode），以及三种不同模型家族。每个比较使用两个评估器配置（共 2×3=6 次重复）或六个评估器配置（opus-4.7 使用 6 个），报告均值和标准差。
- **消融实验**：
  - 评估 RHI 是否也提升训练时缩放（sonnet-4.6 加 RHI vs opus-4.7 无 RHI）。
  - 分析框架组件的贡献（角色、指令、契约、跳转的低维嵌入和余弦相似度）。
  - 研究隐式优化目标：信息论假设（任务互信息、总相关）。
  - 对比单代理多角色与多代理设置（附录 Q2）。
- **充分性**：实验覆盖了三个不同的模型系列（从较弱的 sonnet-4.6 到最强的 opus-4.8），任务跨领域多样，评估使用多个 LLM 判断器和种子，结果具有统计学意义。消融实验提供了对机制的理解。但任务均为合成任务，且仅针对“代码生成”类型，未在真实世界开放式任务上验证。

### 论文的主要结论与发现

1. **少量 RHI 迭代可提升测试时缩放的性能上限**：对低推理努力（high）模型应用 RHI 2-4 次后，在 30 个任务上战胜更强的测试时缩放设置（xhigh/max/ultracode），且优势随模型变强而持续。例如 opus-4.8-high + H[2] 胜过 opus-4.8-ultracode。
2. **RHI 的增益并非来自更长的输出**：输出 token 数在迭代中几乎不变，而性能提升；同时缓存读写（cache read/write）和推理成本下降（最高降低 60%）。
3. **改进源于更有效的上下文管理**：RHI 通过优化契约（contract）和跳转（hop）实现任务特定的智能体间信息流，减少冗余上下文传播，类似稀疏注意力。
4. **RHI 隐式优化目标**：通过信息论分析发现，RHI 增加了外部强调组件（契约和跳转）的任务互信息，同时降低了组件间的冗余（总相关），符合函数特化引导（functional specialization guidance）假设。
5. **用户构建的提示级框架可超越提供商构建的系统级框架**：opus-4.8-high + RHI 胜过 opus-4.8-ultracode 的内置动态多智能体工作流。

### 优点

- **计算轻量**：每次迭代仅需一次代理执行和一次成对评估，远远低于群体搜索方法。
- **收敛快速**：通常 2-4 次迭代即可达到显著提升，适合用户日常使用。
- **无需修改模型权重**：仅在提示级别优化框架，可适用于黑盒模型。
- **结果可解释**：通过嵌入分析和信息论假设揭示了改进机制（任务特定上下文管理），而非单纯靠更长推理。
- **跨模型泛化**：在三种不同能力水平的模型上均有效。

### 不足与局限

- **任务局限性**：实验仅在 30 个合成 ML 研究任务上，未涵盖更广泛的真实开放任务，如日常编程、对话等。
- **评估依赖 LLM 判断器**：虽然使用了多配置和种子，但 LLM 判断可能引入偏差，自身推理质量有限。
- **框架更新依赖 LLM 优化器**：优化器本身是 LLM (L_harness)，其行为和内部偏好不可控，可能带来不确定性和不一致性（论文承认隐式目标而非显式）。
- **未完成循环**：论文仅解决了框架-模型协同演化的前半部分（优化框架以生成更好轨迹），未研究如何将这些轨迹反馈到未来模型训练中。
- **初始框架设计**：需要人工编写初始的多代理框架（H⁰），其质量影响 RHI 最终性能。
- **计算成本分析**：虽然推理成本降低，但 RHI 每次迭代需一次额外的 LLM 评估和框架优化调用（LLM 优化器成本），论文未系统比较总体成本（包括优化开销）与测试时缩放的总成本。
- **可复现性**：依赖特定 API（Claude Code）及其内部缓存机制，可能不易完全复现。

（完）
