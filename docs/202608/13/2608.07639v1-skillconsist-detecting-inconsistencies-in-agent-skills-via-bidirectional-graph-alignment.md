---
title: "SkillConsist: Detecting Inconsistencies in Agent Skills via Bidirectional Graph Alignment"
title_zh: "SkillConsist: 通过双向图对齐检测智能体技能中的不一致性"
authors: "Chaofan Meng, Yuhang Zheng, Yingnan Zhou, Sihan Xu"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07639v1"
tags: ["query:agent-lsh"]
score: 8.0
evidence: 检测技能不一致以规避危险行为的智能体控制技术
tldr: "Agent技能不一致性会引发危险行为或错误选择，现有检测方法受限于预定义类别且难以处理声明与实现混合。本文提出SkillConsist，利用LLM将声明与实现内容分离为行为记录并构建行为图，通过双向图对齐搜索匹配子图并扩展，用图差分识别冲突。在633个基准Skill上包级F1达87.93%，较最优基线提升20.43个百分点，并实现62.52%的定位F1，有效提升检测精度与可解释性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07639v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 867, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07639v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1564, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07639v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 840, \"height\": 512, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07639v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 849, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07639v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 1117, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07639v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 720, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07639v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07639v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 886, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07639v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 747, \"height\": 277, \"label\": \"Table\"}]"
motivation: 现有Agent技能一致性检测依赖预定义类别，难以应对声明与实现的混合及一对多对应，导致检测不准确。
method: 利用LLM分离声明与实现为行为记录，构建行为图，通过双向图对齐扩展匹配子图，再用图差分定位冲突。
result: "在633个技能基准上包级F1达87.93%，较最优基线提升20.43个百分点，定位F1为62.52%。"
conclusion: SkillConsist有效检测Agent技能不一致性，并精确定位冲突，提升安全性与选择可靠性。
---

## 摘要
智能体技能为LLM智能体提供可重用的能力。智能体技能的不一致可能暴露未公开的危险行为或导致错误的技能选择。近期智能体技能研究越来越关注智能体技能一致性检测。现有方法根据预定义类别或声明范围评估行为或安全属性图。最近，PL-HCL使用基于LLM的模型学习元数据、指令和资源之间的一致性。然而，声明与实现行为可能混合在文本和代码中，且简洁的声明可能对应多个相互关联的实现步骤。我们提出SkillConsist来解决这两个挑战。LLM将声明和实现内容分别划分为实现侧和声明侧的行为记录，而静态分析补充实现记录。这些记录分别构成声明行为图和实现行为图。从任一侧的行为记录出发，双向图对齐在另一张图中搜索候选子图，并沿行为关系扩展该子图，直到其完整表达源侧行为。图差异比较识别对齐子图之间的冲突，并输出检测结果。我们基于ClawHub下载量最高的500个公共技能和133个Skill-Inject包构建了包含633个技能的基准。该基准包含319个不一致和314个一致的技能，以及442条局部不一致标注。在该基准上，SkillConsist在包级检测上实现了86.85%的精确率、89.03%的召回率和87.93%的F1分数，与最佳基线相比F1提升了20.43个百分点。在局部定位上，其精确率为67.60%，召回率为58.14%，F1为62.52%。

## Abstract
Agent Skills provide reusable capabilities to LLM agents. Agent Skill inconsistencies can expose undisclosed dangerous behavior or cause wrong Skill selection. Recent Agent Skill research has increasingly examined Agent Skill consistency detection. Existing methods evaluate behaviors or security-property graphs against predefined categories or declared scopes. More recently, PL-HCL uses an LLM-based model to learn consistency across metadata, instructions, and resources. However, declaration and implementation behavior can be mixed across text and code, and a concise declaration can correspond to multiple connected implementation steps. We present SkillConsist to address both challenges. An LLM separates declaration and implementation content into behavior records on the implementation and declaration sides, while static analysis supplements implementation records. These records form declaration and implementation behavior graphs, respectively. Starting from a behavior record on either side, bidirectional graph alignment searches the other graph for a candidate subgraph and expands it along behavior relations until it completely expresses the source-side behavior. Graph differencing identifies conflicts between aligned subgraphs and outputs the detection results. We construct a 633-Skill benchmark from ClawHub's 500 most-downloaded public Skills and 133 Skill-Inject packages. The benchmark contains 319 inconsistent and 314 consistent Skills and 442 localized inconsistency annotations. On this benchmark, SkillConsist achieves 86.85% precision, 89.03% recall, and 87.93% F1 for package-level detection, improving F1 over the best baseline by 20.43 percentage points. For localization, it achieves 67.60% precision, 58.14% recall, and 62.52% F1.

---

## 论文详细总结（自动生成）

好的，我将根据您提供的论文内容，按照要求生成详细的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：LLM（大语言模型）智能体的"技能"（Agent Skills）通常包含两部分：**声明行为**（宣称能做什么）和**实现行为**（实际能做什么）。两者之间的不一致性（inconsistency）会导致严重后果，例如智能体选择错误的技能，或执行超出声明范围的危险操作（如恶意技能隐藏未声明的行为）。
- **研究动机**：
  - 现有的一致性检测方法大多依赖预定义的规则、类别或安全属性图，将自然语言描述与代码片段进行直接比较，这**假设了文本和代码的"角色"已经被明确区分**。
  - 然而，在真实的技能包中，声明和实现行为常常**混合**在自然语言和代码片段中（例如，描述"运行同步脚本并上传结果"的指令实际是执行代码，而命令示例可能是对支持功能的声明）。
  - 同时，**粒度不一致**问题普遍存在：一条简洁的声明可能对应多个连接在一起的实现步骤，反之亦然。
- **整体含义**：该论文旨在提出一种新的方法，能够自动、准确地在完整技能包级别检测和定位"声明-实现"不一致问题，从而提升智能体技能的安全性和选择可靠性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出 **SkillConsist** 框架，通过**双向图对齐**（Bidirectional Graph Alignment）来处理声明与实现之间的行为对应关系，尤其是**一对多**和**多对一**的粒度差异。
- **方法流程**（三个核心阶段）：
  1. **角色分离与行为记录提取**：
     - 使用LLM将混合内容片段按照语义角色（声明/实现）分离，并转换为统一的行为记录格式：`K = <C, O, R, E>`（条件、对象、结果、源位置）。
     - 结合静态分析补充实现侧的行为记录，生成两个独立的记录集合：声明集合`D(S)`和实现集合`M(S)`。
  2. **行为图构建**：
     - 为声明侧和实现侧分别构建有向行为图 `GX = (VX, AX)`，其中节点包括行为记录、对象、公共入口和证据。
     - 通过六种关系边连接相关节点：`APK`（入口可达）、`AKO`（操作对象）、`AEK`（证据支持）、`ARI`（结果传输入）、`ARC`（结果设条件）、`ACC`（条件约束条件）。
     - 通过图路径形成**行为组**（Behavior Groups），一个行为组代表一个完整的、可独立描述的行为单元。
  3. **双向图对齐与差分**：
     - 分别从声明到实现（Q=D, R=M）和从实现到声明（Q=M, R=D）两个方向进行搜索。
     - **候选检索与绑定**：从源侧行为组定位关键记录作为锚点，在目标侧图中检索候选记录并进行硬性校验（入口、对象、范围兼容）。
     - **子图完成**：从绑定记录出发，沿图边扩展，直到源侧行为的所有条件、输入、结果和路径都被覆盖（满足`Align(BQ, H)`公式）。
     - **证明与差分**：通过四值逻辑（Supported/Contradicted/Unknown/Not-Applicable）比较绑定字段值，识别**冲突**（Conflict）；对无对应方的行为，通过**覆盖检查**（Coverage Certificate）判定**未实现**（Unimplemented）或**未声明**（Undeclared）。
- **关键公式/概念**：
  - **三种不一致类型**：`Conflict`（必需声明与实现冲突）、`Unimplemented`（声明存在但无实现对应）、`Undeclared`（实现存在但超出资声明范围）。
  - **一致性判定**：`Consistent(S) ⇔ Complete(S) ∧ I(S) = ∅`，即所有必需声明和范围内实现都被比较且无不一致发现。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集与Benchmark**：
  - 构建了一个包含 **633个智能体技能** 的基准，来源包括：
    - ClawHub上**下载量最高的500个**公共技能。
    - **133个**公开的Skill-Inject恶意/良性技能包。
  - 所有技能经过人工审核，包含 **319个不一致** 和 **314个一致** 的技能。
  - 共包含 **442条** 局部不一致的标注（具体为218个Unimplemented、41个Undeclared、183个Conflict）。
- **评估任务**：
  - **包级检测**：判断技能包是否存在任何不一致。
  - **不一致局部定位**：精确定位到具体的声明或实现位置。
- **对比方法**：
  - **LLM**：直接使用DeepSeek-V4 Pro推断。
  - **LLM+RAG**：使用BM25检索增强的LLM。
  - **C4RLLaMA**：采用LoRA微调的代码LLM（基于CodeLLaMA-7B）。
  - **SKILLSCOPE**：构建安全属性图并使用GPT-5.4检测超出声明范围的行为。
- **附加评估**：在133个Skill-Inject技能（84恶意/49良性）上，将SkillConsist的发现与**MASB**工具结合，评估**恶意技能筛查**性能。

### 4. 资源与算力

- 论文**未明确披露**使用了多少GPU（型号、数量）或模型训练时长。
- 文中仅提及使用的模型名称：**DeepSeek-V4 Pro**（用于主要流程）、**DeepSeek-V4 Flash**（用于消融实验）、**GPT-5.5**（用于恶意技能筛查分级）、**Claude Sonnet 4.6**（用于评估）。所有模型均通过API调用，无本地训练细节。

### 5. 实验数量与充分性

- **实验组数**：论文设计了**丰富的实验**：
  - 按数据源划分（Skill-Inject vs. ClawHub）的总体性能评估。
  - 与四个基线方法的对比。
  - 包含四种变体的**消融实验**（w/o role, w/o expansion, w/o coverage, Flash替换）。
  - 针对217个局部化错误来源的**错误审计**。
  - **恶意技能筛查**的风险分层实验。
- **充分性与客观性**：
  - **优点**：消融实验设计清晰，验证了各个组件（角色分离、子图扩展、覆盖检查）的互补作用；错误审计有助于定性分析局限。
  - **可改进之处**：对比基线的选择上，部分SOTA方法（如BIV、DCIChecker）因无公开实现而被省略，可能削弱了最强竞争对比；LLM基线未采用更强大的专有模型（如GPT-4o/Claude 3.5）。

### 6. 论文的主要结论与发现

- **性能领先**：SkillConsist在包级检测上达到 **87.93% F1**，相比最优基线大幅提升 **20.43个百分点**（在ClawHub上提升至86.71% vs 68.83%）。
- **局部定位有效**：局部定位F1为 **62.52%**，远高于基线（最好基线仅18.06%）。
- **组件互补**：角色分离能够提升实际检出能力；子图扩展对定位多步行为至关重要；覆盖检查有效抑制了误报。
- **RAG的局限**：RAG虽可提升召回率，但会严重损害局部定位精度，说明"检索证据"并不等于"建立行为对应关系"。
- **安全性价值**：将一致性风险分级后，有助于提升恶意技能筛查的召回率（在保持96%以上精确率的同时，最高可提升26.19%的相对召回率），且额外动态验证成本极低。

### 7. 优点

- **问题定义的严谨性**：对"一致性"进行了形式化定义，区分了三种不一致类型，并引入"必需声明"与"范围内实现"的边界条件。
- **方法设计的创新性**：
  - **角色分离**解决混合内容问题。
  - **图构建与双向对齐**优雅地处理了声明与实现间的**粒度差异**（一对多/多对一）。
  - **可解释性强**：所有发现都附带源位置（文件和行号），便于审计和修复。
- **实验评估扎实**：构建了大规模人工审核基准，并进行了多维度评估（包级、定位、消融、错误归属、应用场景）。

### 8. 不足与局限

- **对LLM抽取质量的依赖**：行为记录的提取依赖于DeepSeek-V4 Pro的推理能力，实验中用Flash版本替代时性能显著下降（F1从87.93%降至69.18%），表明方法对模型能力敏感。
- **局部定位F1仍属中等**：62.52%的F1说明在挑战性场景下仍有许多错误，其中**跨工件对象和行为对应**是主要难点。
- **覆盖检查的严格性**：缺失侧（Unimplemented/Undeclared）行为的判定依赖"完全覆盖证书"，在静态分析或受控执行不足时，容易产生Unknown或误报（例如未声明功能的FP多源于覆盖不完整或对象别名）。
- **基准来源局限**：仅使用ClawHub和Skill-Inject数据，可能无法完全代表生态中所有类型和复杂度的技能包。
- **资源披露不足**：未提供完整的实验硬件配置，对于需要本地部署或大规模复现的场景，难以估算成本。
- **未进行跨模型验证**：未测试不同主模型（如GPT-4o、Claude）对框架鲁棒性的影响，泛化能力仍待验证。

（完）
