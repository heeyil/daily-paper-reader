---
title: "SWE-Router: Routing in Multi-turn Agentic Software Engineering Tasks"
title_zh: SWE-Router：多回合智能软件工程任务中的路由
authors: "Seongho Son, Sangwoong Yoon, Jiahua Tang, Shuhan Wang, Lorenz Wolf, Ilija Bogunovic"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00053v1"
tags: ["query:moe-routing"]
score: 9.0
evidence: 基于部分轨迹在智能体软件工程任务中进行路由
tldr: 针对多轮代理软件工程任务中直接路由所有任务给最强模型成本高的问题，现有路由仅基于任务描述存在信息瓶颈。SWE-Router让廉价模型先执行几轮探索，根据部分轨迹决策是否切换到昂贵模型。理论证明该策略贝叶斯最优且不劣于直接路由，实验在多种强弱模型组合上显著提升成本效率并保留强模型大部分性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00053v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1724, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00053v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1754, \"height\": 617, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00053v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1165, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00053v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1163, \"height\": 798, \"label\": \"Table\"}]"
motivation: 现有LLM路由仅基于任务描述，在代理场景中面临信息瓶颈，无法区分相似问题中的简单与复杂修复。
method: 提出SWE-Router，让廉价模型先执行若干探索轮次，基于部分轨迹进行价值计算，决定继续使用廉价模型或升级到昂贵模型。
result: 在多种强弱LLM组合上，SWE-Router大幅提升成本效率，同时保持强模型大部分性能。
conclusion: 基于部分轨迹的路由策略可有效降低多轮代理软件工程任务的成本，具有理论最优性。
---

## 摘要
嵌入在多回合智能框架中的大型语言模型正在重塑软件工程（SWE），但当许多问题可以通过廉价修复解决时，将每个任务路由到前沿模型是浪费的。现有的LLM路由器仅根据任务描述进行操作，这在智能环境中继承了信息论的贝叶斯误差下限：类似的问题可能隐藏着局部的打字错误或多模块重构，而提示无法区分两者。我们引入了SWE-Router，一种基于价值的时序方法，允许廉价模型运行几个探索性回合，并在决定是继续廉价处理还是升级到昂贵模型之前读取产生的部分轨迹。我们提供了一个贝叶斯最优定理，表明基于部分轨迹的条件化永远不会损害路由，并且每当探索具有信息性时严格更好。在跨越当代成本-能力边界的弱弱和强弱模型对之间，我们展示了SWE-Router大大提高了SWE任务的成本效率，同时保持了强模型的大部分性能。我们还发布了一个多LLM轨迹数据集，允许复现我们的轨迹级路由。

## Abstract
Large language models (LLMs) embedded in multi-turn agentic harnesses are reshaping software engineering (SWE), but routing every task to a frontier model is wasteful when many issues admit cheap fixes. Existing LLM routers operate on the task description alone, which inherits an information-theoretic Bayes-error floor in agentic settings: a similar issue can hide either a localized typo or a multi-module refactor, and the prompt does not separate the two. We introduce SWE-Router, a value-based temporal approach that lets a cheap model run for a few exploratory turns and reads the resulting partial trajectory before deciding whether to continue cheaply or to escalate to an expensive model. We provide a Bayes-optimality theorem showing that conditioning on the partial trajectory never harms routing and is strictly better whenever exploration is informative. Across the LLM pairs of weak and strong models spanning the contemporary cost--capability frontier, we show that SWE-Router greatly improves the cost efficiency of SWE tasks, while maintaining the majority of the performances of the stronger model. We additionally release a multi-LLM trajectory dataset which allows reproduction of our trajectory-level routing.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在多轮智能体软件工程（SWE）任务中，现有路由方法仅基于任务描述（问题文本）决定调用哪个LLM，面临信息论上的贝叶斯误差下限：相似描述的问题可能包含简单打字错误或复杂多模块重构，仅凭提示无法区分。直接对所有任务使用前沿模型（如Gemini 3 Pro）成本浪费，而使用廉价模型可能导致解决率下降。
- **整体含义**：论文提出一种利用智能体早期探索轨迹信息来改进路由决策的框架，在保持强模型大部分性能的同时大幅降低成本，为多轮agentic任务中的高效成本-性能平衡提供了新思路。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：让廉价弱模型先进行若干探索回合，收集部分轨迹（包括思考、动作、观察），然后基于该轨迹预测弱模型最终能否成功，据此决定是继续使用弱模型还是切换至强模型。
- **关键技术细节**：
  - **价值函数**：使用预训练LLM（Qwen2.5-Coder-7B-Instruct）加上二分类头，通过LoRA微调，以二元奖励（通过/未通过测试）作为监督信号，训练得到 $ \hat{r}_1(\mathcal{T}_{\le K, 1}) $ 表示弱模型从部分轨迹出发能成功解决的概率。
  - **路由规则**（单边路由简化版）：
    - 弱模型运行 $K$ 步后，计算 $ \hat{r}_1 \ge \lambda'' $。
    - 若满足，则继续使用弱模型直到结束；否则从原始提示重启强模型。
    - 阈值 $ \lambda'' $ 在验证集上调节。
  - **理论保证**（定理4.1）：基于部分轨迹 $S_t$ 的条件化贝叶斯最优期望效用不低于仅基于 $Q$ 的，且当探索提供信息时严格更好。
- **算法流程（部署时）**：
  1. 初始化轨迹 $ \mathcal{T} \leftarrow [q] $。
  2. 弱模型运行 $K$ 步，每一步生成思考、动作、观察加入轨迹，若中途提交则直接返回。
  3. 用价值头计算 $ \hat{r}_1(\mathcal{T}) $。
  4. 若 $ \hat{r}_1 \ge \lambda'' $，弱模型继续；否则强模型从 $q$ 重新开始。

### 3. 实验设计

- **数据集**：SWE-Smith（训练/验证/测试，按仓库划分无重叠）和 SWE-Bench Verified（跨分布评估）。
- **基准**：三种基于嵌入的提示级路由器（Logistic回归、k-NN、XGBoost on text-embedding-3-large），以及非时序路由（$K=0$ 的SWE-Router变体），随机分配蒙特卡洛参考（400次置换）。
- **LLM对**：弱模型 = gpt-5-mini / deepseek-v3.2，强模型 = gemini-3-pro-preview，在 mini-SWE-agent 中生成轨迹。
- **评估指标**：AUROC（价值头判别质量）、Route-AUC（成本-解决率曲线归一化面积）。
- **实验数量**：
  - 两个弱-强模型对 × 三个数据集（SWE-Smith val, test, SWE-Bench Verified）。
  - 每个对中测试 K=1,2,3,4 四种探索步数，并与三种嵌入基线、非时序基线对比。
  - 列出完整结果表（Table 2）和成本-解决率曲线图（Figure 2）。
- **充分性与公平性**：实验覆盖不同模型组合、多种K值、不同分布（同源/跨源），使用标准评估协议，阈值在验证集上调优，测试集独立，对比基线包含当前主流方法。但SWE-Smith测试集上Route-AUC提升不如SWE-Bench显著，说明存在分布偏移，但整体实验设计合理、结果客观。

### 4. 资源与算力

- 文中**未明确说明**使用的GPU型号、数量、训练时长。
- 价值函数微调细节：Qwen2.5-Coder-7B-Instruct + LoRA（r=32, α=64, dropout 0.05），5 epochs，lr 5e-5，cosine schedule with 50-step warmup，batch size 16，context length 8192 tokens。
- 轨迹数据采集：在SWE-Smith和SWE-Bench上运行多个LLM，共约100万美元算力成本（基于论文表1的总成本估计，实际算力细节缺失）。

### 5. 实验数量与充分性

- **分组数**：2类弱-强对 × 4种K值 × 3个数据集 = 24组主要实验，加上3种嵌入基线 × 3数据集 = 9组，以及非时序基线。
- **消融**：通过K=0与非时序路由器对比，验证探索回合的必要性。
- **鲁棒性**：在跨数据集（SWE-Bench Verified）上验证泛化能力，并使用随机置换检验（400次）确保统计显著性。
- **结论**：实验较为充分，客观性较好，但未进行更大规模（如更多弱/强模型、更多K值）的消融，也未探索多轮切换或在线适应。

### 6. 论文的主要结论与发现

- 基于部分轨迹的时序路由（SWE-Router）能够显著降低多轮agentic SWE任务的总成本，同时保持强模型大部分解决率。
- 在SWE-Bench Verified上，Route-AUC相比最强基线提升至少12个百分点（弱模型为gpt-5-mini时+12pp，deepseek-v3.2时+15.3pp）。
- 理论上证明基于更多信息（部分轨迹）的条件化路由不会比仅基于提示差，且探索有益时严格更优。
- SWE-Router的曲线有时甚至超过始终使用强模型的点，说明利用弱模型解决强模型失败的实例的互补性。

### 7. 优点

- **方法创新**：首次提出利用智能体中间轨迹进行路由，突破了仅基于提示的信息瓶颈，为多轮agentic任务路由提供新范式。
- **理论支撑**：提供贝叶斯最优性定理，证明方法在信息论意义上的优越性。
- **实践高效**：简单易用的单边路由规则，仅需训练一个价值头，计算成本低。
- **开源贡献**：发布多LLM轨迹数据集，促进可复现研究。

### 8. 不足与局限

- **探索代价固定**：弱模型K步探索成本在任何情况下都需支付，即使可能导致浪费（如弱模型已耗时但最终仍需切换）。
- **K值敏感**：实验中K=1~4，未系统研究更大K或自适应K，阈值需在验证集调优。
- **泛化局限**：仅在SWE领域验证，未推广到其他多轮agentic任务（如客户服务、对话系统）。
- **安全风险**：论文提及可能被滥用以绕过安全模型，但未提供解决方案（如安全感知路由）。
- **算力细节缺失**：未报告GPU型号、训练时长等，可复现性受限。
- **分布偏移**：在SWE-Smith测试集上部分Route-AUC提升不显著，说明跨仓库场景下价值头的泛化能力有待加强。

（完）
