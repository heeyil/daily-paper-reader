---
title: "Experience Graphs: The Data Foundation for Self-Improving Agents"
title_zh: 经验图：自我改进智能体的数据基础
authors: "Gang Liao, Yujia He, Abdullah Ozturk, Zhouyang Li, Ying Wang, Zhitong Guo, Hongsen Qin, Yaobin Qin, Tao Yang, Zewei Jiang, Dianshi Li, Jort Gemmeke, Jiangyuan Li, Liyuan Li, Nathan Yan, Masha Basmanova, Uladzimir Pashkevich, Matt Steiner, Pedro Pedreira, Rob Fergus, Anirudh Goyal, Carole-Jean Wu, Gaoxiang Liu, Andrew Witten, Daniel J. Abadi"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29823v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 基于经验图的自改进智能体数据基础
tldr: "长期智能体任务产生结构化的经验图，但现有框架将其视为临时状态。Trellis将经验图作为一等数据库状态，支持查询、复用和训练。在Meta的KernelEvolve中，跨会话复用实现约10倍加速和52%更低token成本。这使推理时搜索从一次性计算变为可累积的持久化资产。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1616, \"height\": 582, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1557, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 453, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29823v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 742, \"height\": 347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29823v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1743, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29823v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1805, \"height\": 457, \"label\": \"Table\"}]"
motivation: 现有智能体框架将经验视为一次性状态，无法恢复、跨用户查询或转化为训练数据，限制了自改进能力。
method: Trellis将经验图作为可查询的数据库状态，实现前端选择为查询、跨会话复用为图检索、训练数据提取为物化视图。
result: "在KernelEvolve中，跨会话复用达到约10倍加速，token成本降低52%，验证了经验图数据库的有效性。"
conclusion: 经验图数据库将推理搜索从临时计算转为持久机构资产，推动智能体累积性自改进。
---

## 摘要
数据库社区多次通过认识到新工作负载需要新系统架构来推动技术前沿。我们认为，长周期智能体任务——代码生成、科学发现、硬件设计——正是这样一种工作负载。这些智能体进行探索：它们生成工件、执行工具、观察失败、分支并在数百步中进行修复。这种搜索会产生一个结构化的对象，我们称之为经验图：可执行工件、工具输出、奖励、兄弟比较和因果谱系。然而，现有的智能体框架将这种经验视为一次性状态——JSON检查点和会话日志，这些在崩溃后无法恢复，无法跨用户查询，也无法物化为训练数据。我们提出Trellis：一个将经验图视为一等公民、可管理、可查询的数据库状态的数据基础。核心洞察是，对经验图的搜索是一种数据库访问模式。前沿选择是一种查询，跨会话重用是向量种子化的图检索，训练数据提取是物化视图，而重建智能体在过去任何步骤中的知识是时间旅行查询。当数据库拥有经验图时，智能体变为无状态计算，崩溃恢复、水平扩展和闭环训练飞轮作为架构副产品出现。我们在Meta的生产加速器内核优化器KernelEvolve中验证了这一设计，其中跨会话重用达到目标加速约10倍，同时令牌成本降低52%。更广泛地，Trellis将推理时搜索从一次性计算转变为持久的机构资产：日志使数据库可靠；经验图可能使智能体具有累积性。

## Abstract
The database community has repeatedly advanced the state of the art by recognizing that new workloads demand new system architectures. We argue that long-horizon agentic tasks -- code generation, scientific discovery, hardware design -- are such a workload. These agents explore: they generate artifacts, execute tools, observe failures, branch, and repair over hundreds of steps. This search produces a structured object we call an experience graph: executable artifacts, tool outputs, rewards, sibling comparisons, and causal lineage. Yet existing agent frameworks treat this experience as disposable state -- JSON checkpoints and session logs that cannot be recovered after a crash, queried across users, or materialized into training data. We propose Trellis: a data foundation that treats the experience graph as first-class, governed, queryable database state. The core insight is that search over experience graphs is a database access pattern. Frontier selection is a query, cross-session reuse is vector-seeded graph retrieval, training-data extraction is a materialized view, and reconstructing what an agent knew at any past step is a time-travel query. When the database owns the experience graph, agents become stateless compute, and crash recovery, horizontal scaling, and a closed-loop training flywheel emerge as architectural byproducts. We ground the design in KernelEvolve, a production accelerator-kernel optimizer at Meta, where cross-session reuse reaches a target speedup roughly 10x faster at 52% lower token cost. More broadly, Trellis turns inference-time search from disposable computation into a durable institutional asset: logs made databases reliable; experience graphs may make agents cumulative.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：长周期智能体任务（如代码生成、科学发现、硬件设计、安全研究）不是一次推理就能完成的，而是通过迭代搜索——生成工件、执行工具、观察失败、分支、修复——形成结构化的探索过程。每一步都产生可执行工件、工具输出、目标奖励、兄弟比较和因果谱系，共同构成一个 **“经验图”（experience graph）**。然而，现有的智能体框架（如Claude Code、Codex）将这种经验视为一次性状态——以JSON检查点和会话日志存储，崩溃后无法恢复，无法跨用户查询，无法系统地转化为训练数据。这限制了自改进智能体的累积性学习能力。
- **整体含义**：作者主张，**智能体记忆本质上是一个数据库问题**。通过将经验图作为一等公民、可管理、可查询的数据库状态，可以自然实现崩溃恢复、水平扩展、跨会话复用和闭环训练飞轮。这项工作将数据库社区的设计方法论引入智能体系统，为下一代自改进智能体提供数据基础设施。

## 2. 论文提出的方法论

### 核心思想
- **两层循环架构**：内循环是技能驱动的智能体会话（LLM生成、执行、评估、持久化）；外循环是搜索编排策略（MCTS、进化搜索、贪心等），决定下一步扩展哪个前沿节点。两层通过持久化数据基础（Trellis）解耦，智能体变为无状态计算。
- **核心洞察**：对经验图的搜索是一种数据库访问模式。前沿选择是查询，跨会话复用是向量种子化的图检索，训练数据提取是物化视图，重建历史状态是时间旅行查询。

### 关键技术细节
1. **逻辑模型**：四表结构（tasks, sessions, nodes, prompt_histories），支持算法无关、任务无关、技能无关的模式。大型工件存储在对象存储中，通过引用连接。
2. **上下文作为托管状态**：prompt_history表不仅用于训练轨迹回放，还允许子节点继承父节点会话或通过祖先查询重构上下文，避免令牌浪费和重复修复。
3. **物理架构**：分离存储与计算。使用Axiom（成本优化器）和Velox（向量化执行引擎）统一处理SQL、Cypher和向量检索，路由到不同后端（操作存储、向量索引、列存仓库）。
4. **向量种子图扩展**：给定新任务，先通过向量相似度找到语义相似的旧任务，再通过关系连接找到最优探索节点，然后进行变长图遍历提取完整轨迹，实现跨会话知识复用。
5. **并发多智能体搜索**：多个工人同时从数据库中读取前沿节点，独立执行内循环，插入结果；MCTS反向传播（更新访问计数和累积奖励）需要原子性，但容忍最终一致性。
6. **多版本状态与时间旅行**：节点字段（如访问计数）会原地修改，Trellis捕获每次字段级变更到一个轻量级变更日志（按逻辑步骤号键控），支持任何过去状态的重建，用于训练数据的时间一致性。
7. **训练数据作为物化视图**：SFT轨迹（根到叶子路径）、DPO偏好对（兄弟节点比较）、GRPO组（从持久缓冲区采样状态并扩展N个子节点）均通过数据库查询从经验图中提取，无需额外管线。优势通过SQL窗口函数计算。

## 3. 实验设计

- **场景与数据集**：使用Meta的生产环境内核优化器 **KernelEvolve**，它针对四个硬件平台（NVIDIA、AMD、MTIA、CPU）优化加速器内核。经验图积累了大量会话和任务。
- **Benchmark**：对比三种配置：
  - 基线（无跨会话记忆）
  - 跨会话记忆注入率 p=0.1
  - 跨会话记忆注入率 p=0.5
- **评估指标**：
  - 最佳适应度（speedup）随评估步骤的变化
  - 有效节点（达到基线加速比）与错误节点（触发调试重试）的比例
  - 每个有效节点的令牌成本
  - 达到1.2×加速比的步骤数
- **对比方法**：仅对比自身加记忆与不加记忆，未与其他智能体记忆系统（如Mem0、Graphiti）进行直接比较。

## 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量或训练时长。实验在KernelEvolve的生产环境中运行，但具体硬件细节缺失。作者仅提到会话步骤预算为100步，每步生成节点，并进行了三次独立会话取平均。

## 5. 实验数量与充分性

- **实验数量**：主要报告了三组独立会话（每种配置三次）的平均结果。额外提及了将Trellis迁移到MTIA硅验证任务的示例，但未给出定量结果。
- **充分性评价**：实验展示了崩溃恢复、跨会话记忆在收敛速度和成本上的显著收益，但覆盖范围有限：
  - 只有一个任务场景（内核优化），未在代码生成、科学发现等其他长周期任务上验证。
  - 未与其他记忆系统（如文件式记忆、Mem0、Graphiti）定量对比。
  - 缺乏端到端训练飞轮（SFT/DPO/GRPO）的实际效果验证。
  - 未测试大规模并发搜索的一致性开销。
- **客观性**：作者承认实验是初步的，并坦率指出了探索-锚定权衡，但可能因只展示有利结果而存在偏差风险。

## 6. 论文的主要结论与发现

1. **崩溃恢复与无状态性**：基于数据库的状态使得任何工人都能从最后一次提交的节点恢复，无需检查点反序列化，生产中实现了无节点丢失的自动恢复。
2. **跨会话记忆显著提升效率**：
   - 错误节点率从55%（无记忆）降至34%（p=0.1）和21%（p=0.5）。
   - 达到1.2×加速比的步骤数从51步降至约5步，即 **10×加速**。
   - 令牌成本降低52%（因减少调试重试循环）。
3. **探索-锚定权衡**：p=0.5时搜索更稳定但多样性下降（只有8个策略组合 vs. 20个），最高适应度低于无记忆（1.36× vs. 1.49×）。p=0.1最优，平衡了收敛速度和探索多样性。
4. **训练数据无需重新生成**：生产会话同时作为服务操作和训练数据生成器，无需单独管线，保存完整谱系（任务、硬件、算法）。

## 7. 优点

- **新颖的系统视角**：首次将智能体工作负载系统地定义为数据库访问模式，提供了从临时状态到持久化、可查询、可训练的架构转变。
- **全面的设计**：覆盖了智能体记忆的多个维度——图遍历、向量检索、结构化过滤、事务性更新、时间旅行、数据治理、训练视图物化——并统一在单一查询层下。
- **生产验证**：基于Meta的真实生产系统KernelEvolve，展示了实际收益，具有说服力。
- **领域泛化性**：通过改变适应度函数和技能，成功迁移到MTIA硅验证场景，暗示方法可适用于科学发现、安全研究等。
- **具前瞻性的研究议题**：明确提出多个开放问题（多模态查询规划、并发树搜索一致性、物理设计、治理视图维护、检索策略、双时间记忆、学习控制、多智能体科学社会），为社区指明方向。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅在一个具体任务（内核优化）上评估，泛化性有待验证。
  - 未与其他记忆系统（如Mem0、Graphiti、文件式记忆）进行公平定量对比。
  - 缺少对更大规模经验图（例如数千会话）的性能和一致性测试。
  - 未展示端到端训练迭代（SFT/DPO/GRPO）对模型质量的提升效果。
- **潜在偏差风险**：
  - 作者来自Meta，实验数据来自其内部系统，可能偏向有利结果。
  - 没有独立第三方复现。
- **工程细节缺失**：
  - 未报告硬件配置、会话数量规模、图大小等关键参数。
  - 未量化时间旅行和并发控制的开销。
- **理论分析不足**：虽然提出了多个开放问题，但缺乏对一致性模型、查询计划成本模型等核心问题的严谨理论分析。

（完）
