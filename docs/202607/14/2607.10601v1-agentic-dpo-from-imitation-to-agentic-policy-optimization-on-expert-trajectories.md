---
title: "Agentic-DPO: From Imitation to Agentic Policy Optimization on Expert Trajectories"
title_zh: Agentic-DPO：从模仿到基于专家轨迹的智能体策略优化
authors: "Yixiong Chen, Alan Yuille"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10601v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 通过轨迹偏好优化实现代理自我改进
tldr: "现有LLM agent通常用SFT模仿专家轨迹，但只学习动作序列，未能训练选择正确动作。Agentic-DPO将专家轨迹转化为状态条件下的偏好监督，通过一步采样并对比错误动作，结合PPA保持策略不变，无需在线环境或奖励模型。在tau-bench上，9B模型准确率从21.7%提升至41.4%，媲美在线GRPO。该方法表明专家轨迹可低成本实现agent策略优化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10601v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1433, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1413, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 392, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10601v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 986, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 591, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 539, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1410, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1447, \"height\": 158, \"label\": \"Table\"}]"
motivation: SFT训练LLM agent仅模仿专家动作序列，未能学习在状态中选择正确动作，需要低成本离线优化方法。
method: Agentic-DPO将专家轨迹转为状态级偏好监督：从当前状态一步采样，以错误动作作负样本，使用DPO偏好目标对比。引入PPA保持策略不变。
result: "在StableToolBench、tau-bench retail和Mind2Web上，Agentic-DPO一致提升agent性能。tau-bench准确率从21.7%提升至41.4%（9B模型），匹配在线GRPO。"
conclusion: 专家轨迹可转化为状态级动作偏好，支持无需环境交互的轻量级离线策略优化，降低训练成本并提升agent决策能力。
---

## 摘要
大型语言模型（LLM）智能体通常使用监督微调（SFT）从专家轨迹进行训练，该方法将多轮智能体行为视为普通文本模仿。这种方案简单且成本低，但它只学习模仿专家动作的序列，而非训练智能体在每个状态下针对可能的错误选择正确动作。现有缓解此问题的方法包括偏好学习或强化学习，但通常需要高成本的环境 rollout 和奖励模型。我们提出 Agentic-DPO，一种轻量级离线智能体策略优化方法，将专家轨迹转化为状态条件偏好监督。在每个专家动作状态下，Agentic-DPO 从当前状态采样一步动作，将可能的错误动作视为负样本，并使用 DPO 风格的偏好目标将其与专家动作进行对比。为避免在偏好学习中混合策略和模式，我们引入策略保持增强（PPA），它在多种模式下渲染相同的潜在轨迹，同时保持专家策略固定。Agentic-DPO 无需在线环境 rollout、奖励模型或全轨迹学生探索。我们在 StableToolBench、tau-bench retail 和 Mind2Web 上进行实验，Agentic-DPO 在不同模型规模上一致提升了超越模仿的智能体性能。特别地，对于 9B 模型，它将 tau-bench 准确率从 21.7%（SFT）提升至 41.4%，在相同主干网络下匹配了在线 GRPO，且仅需步骤级 rollout 并在梯度步骤中无需环境交互。结果表明，当将专家轨迹从演示转化为状态级动作偏好时，可以支持低成本的智能体策略优化。Agentic-DPO 的代码已发布在 https://github.com/Schuture/Agentic-DPO。

## Abstract
Large Language Model (LLM) agents are commonly trained from expert trajectories using supervised fine-tuning (SFT), which treats multi-turn agent behavior as ordinary text imitation. This recipe is simple and low-cost, but it only learns to imitate the sequence of expert actions, rather than training the agent to choose the right action against plausible mistakes at each state. Existing methods to mitigate this problem include preference learning or reinforcement learning, but they usually need high-cost environment rollouts and reward models. We propose Agentic-DPO, a lightweight offline agent policy optimization method that turns expert trajectories into state-conditioned preference supervision. At each expert action state, Agentic-DPO samples a one-step action from the current state, treats plausible wrong actions as negatives, and contrasts them with the expert action using a DPO-style preference objective. To avoid mixing both policy and schema in preference learning, we introduce Policy-Preserving Augmentation (PPA), which renders the same latent trajectory under multiple schemas while keeping the expert policy fixed. Agentic-DPO requires no online environment rollout, reward model, or full-trajectory student exploration. We conduct experiments across StableToolBench, tau-bench retail, and Mind2Web, where Agentic-DPO consistently improves agents at different model scales beyond imitation. In particular, it raises tau-bench accuracy from 21.7% (SFT) to 41.4% for a 9B model, matching online GRPO under the same backbone with only step-level rollouts and without environment interaction during gradient steps. The results suggest that expert trajectories can support low-cost agentic policy optimization when converted from demonstrations into state-level action preferences. Code for Agentic-DPO is released at https://github.com/Schuture/Agentic-DPO.

---

## 论文详细总结（自动生成）

# Agentic-DPO 论文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：现有LLM agent通常使用监督微调（SFT）从专家轨迹中训练，但SFT仅学习模仿专家动作的token序列，无法训练agent在每个状态下针对可能的错误选择正确动作。这种“行为克隆”的缺陷在agent任务中尤为显著，因为agent轨迹本质上是状态条件决策序列。
- **背景**：偏好学习或强化学习（如RLHF、GRPO）可以缓解此问题，但通常需要高成本的环境rollout、奖励模型训练或全轨迹探索。论文旨在探索一种低成本、离线的agent策略优化方法，将专家轨迹直接转化为状态级偏好监督信号。
- **整体含义**：提出Agentic-DPO，证明专家轨迹本身即可支持有效的策略优化，无需在线环境交互，为低成本agent训练开辟了新路径。

## 2. 论文的方法论

### 核心思想
- 将每个专家动作视为当前状态下的偏好动作，在同一状态从当前学生策略采样一步动作作为负样本，形成偏好对，使用DPO风格的损失函数进行对比学习。
- 通过策略保持增强（Policy-Preserving Augmentation, PPA）避免偏好学习混入模式（schema）干扰，使梯度更聚焦于潜在决策差异。

### 关键技术细节
1. **状态条件偏好构建**：
   - 对专家轨迹中的每步 `(st, u_t^+)`，采样一个模式视图 `ϕ`，渲染状态 `s_ϕ^t`。
   - 从当前学生策略采样K=4个候选动作，过滤掉无法解析或等价于专家动作的候选。
   - 选择剩余候选中学生log概率最高的作为负样本 `u_t^-`（最难区分错误动作）。
2. **损失函数**：
   - 长度缩放的DPO损失：`L_ADPO = -log σ[β_eff * (log πθ(a^+|s)/πθ(a^-|s) - log πref(a^+|s)/πref(a^-|s))]`，其中 `β_eff = β / max(|a^+|,|a^-|)^α, α=0.5`，防止长动作支配。
   - 联合SFT锚定损失：`L_train = L_ADPO + λ * L_SFT`，保持专家动作概率不被过度压低。
3. **策略保持增强（PPA）**：
   - 动作渲染重写：同一潜在动作以不同格式（ReAct、JSON列表）、重命名工具等方式呈现。
   - 状态上下文重写：在历史中注入先前的错误动作及修正，但保持当前步的专家决策不变。
   - 训练时，每个minibatch随机采样视图 `ϕ`，确保偏好方向与潜在决策对齐。

### 算法流程（文字描述）
1. 在专家轨迹上使用在线PPA进行SFT热身，冻结参考策略 `πref`。
2. 进行R轮（论文默认R=5）负样本刷新循环：
   - 快照当前学生策略，从专家数据中选取子集。
   - 对每个专家状态，采样K个候选，选择最难负样本，构建偏好缓冲区。
   - 进行T步梯度更新：对每个三元组采样PPA视图，用锚定损失训练。
3. 返回最终策略。

## 3. 实验设计

### 数据集/场景与Benchmark
- **StableToolBench**：工具使用场景，评估canonical和perturbed准确率。
- **τ-bench retail**：长程用户交互工具使用，评估任务成功率。
- **Mind2Web**：网页GUI grounding，评估跨任务/跨网站/跨域的平均步骤成功率。
- 额外零样本测试：在BFCL-v3（函数调用榜单）上评估OOD泛化。

### 对比方法
- 基线：Base原始模型、SFT、PPA+SFT（仅多视图正样本）、DFT（奖励修正SFT）、ETO（轨迹级DPO）、GRPO（在线分组RL）。
- 消融：移除SFT热身、移除SFT锚定、移除动作渲染PPA、移除恢复上下文PPA、移除所有PPA。

### 模型规模
- 主干网络：Qwen3.5-2B/4B/9B，外加Gemma3-4B用于消融验证。

## 4. 资源与算力
- 训练平台：4块A6000 GPU。
- 超参数：批量大小16，负样本刷新轮次R=5，每轮T步梯度更新（未明确T值），K=4，β=0.008，λ=0.5。
- **论文未明确说明训练总时长**，但提及Agentic-DPO每步成本为SFT的1.6倍，而GRPO为13.6倍，可见其计算开销显著低于在线RL。

## 5. 实验数量与充分性

### 实验数量
- **主实验**（表1）：3个数据集 × 3个模型规模，每个结果3个随机种子平均，共27组+对比方法。
- **消融实验**（表2）：Qwen3.5-2B和Gemma3-4B上8种配置，覆盖canonical和perturbed。
- **鲁棒性分析**（表3）：区分in-class（PPA相关）和held-out（新扰动）两类扰动。
- **零样本OOD**（表4）：在BFCL-v3上评估，对比Base、SFT、Agentic-DPO。
- **缩放实验**（图2）：K从1~16，数据量从25%~100%。
- **刷新轮次分析**（图3）：R=0~5，三个模型规模。
- **扰动集构建**（附录C）：9种算子 × 3难度，162个文件。

### 充分性与公平性
- 实验覆盖多个任务类型（工具使用、长程交互、GUI操作），模型规模范围较广。
- 对比方法涵盖SFT、离线偏好学习（ETO）、在线RL（GRPO），基线选择合理。
- 消融实验独立验证了各组件（SFT热身、锚定、PPA两家族）的必要性，结果清晰。
- 种子方差报告充分（标准差），统计稳健。
- **局限性**：未在更大模型（如70B）或更复杂任务（如SWE-bench）上验证；GRPO仅在部分benchmark上运行（Mind2Web静态轨迹无法提供步骤奖励）。

## 6. 主要结论与发现
- Agentic-DPO在所有三个基准和三种模型规模上一致超越SFT和所有对比基线，平均提升超过25点（2B）/22点（4B）/18点（9B）相对于SFT。
- 在τ-bench retail上，9B模型达到41.4%成功率，匹配甚至略优于在线GRPO（40.0%），且训练成本大幅降低。
- PPA和SFT锚定是稳定训练的关键：移除SFT热身导致完全失败（0%准确率），移除任一PPA家族会扩大canonical与perturbed之间的差距。
- Agentic-DPO对负样本数量不敏感（K=2~4即饱和），且数据效率高（仅25%数据即可匹配PPA+SFT全量性能）。
- 较大模型需要更少的负样本刷新轮次（9B只需1轮），说明其初始采样已接近有用决策边界。

## 7. 优点
- **轻量级离线方法**：无需在线环境rollout、无需奖励模型、无需全轨迹学生探索，仅需一步采样，显著降低训练成本。
- **利用专家轨迹结构**：将轨迹视为状态条件决策序列，而非普通文本，构建了更有针对性的偏好信号。
- **PPA设计精巧**：通过多视图渲染分离策略决策与模式格式，避免了DPO梯度被无关格式差异主导。
- **SFT锚定**直观有效：在理论上证明与DPO梯度方向一致，实践中稳定了训练，防止专家概率被过度压低。
- **鲁棒性强**：在多种扰动和零样本OOD场景下均表现优异，且方差小。
- **可扩展性**：在不同模型规模、不同任务类型上均一致有效。

## 8. 不足与局限
- **离线方法固有局限**：偏好仅能从专家轨迹中已有的状态构建，无法探索学生策略可能到达的新状态，这与在线RL的本质差异。
- **专家数据依赖**：当专家轨迹覆盖不足或分布偏移时，构造的偏好可能不全面，影响泛化。
- **未在更大模型上验证**：实验最大规模为9B，在更大模型（如70B/130B）或更复杂现实任务（如SWE-bench、WebArena）上的有效性未知。
- **扰动评估覆盖有限**：虽然设计了9种扰动，但均为同分布内的小范围变换，对真实世界对抗性扰动（如恶意格式攻击）的鲁棒性未研究。
- **训练时间未明确报告**：虽然对比了资源倍数，但缺乏绝对训练时长、GPU小时数等具体数据，不利于公平比较。
- **超参数敏感度**：β、λ、R、K等超参数仅在固定值下测试，未系统探索其相互作用（如R与模型规模的关系已部分分析，但其他参数类似分析缺失）。
- **理论分析较浅**：仅提供了局部梯度对齐的证明，缺少全局收敛性或泛化误差界的理论保证。

（完）
