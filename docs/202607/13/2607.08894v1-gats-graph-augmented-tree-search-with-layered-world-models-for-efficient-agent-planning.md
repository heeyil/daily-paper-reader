---
title: "GATS: Graph-Augmented Tree Search with Layered World Models for Efficient Agent Planning"
title_zh: GATS：基于分层世界模型的图增强树搜索用于高效智能体规划
authors: "Maureese Williams, Dymitr Nowicki"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08894v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 图增强树搜索与分层世界模型，用于高效的长时域代理规划
tldr: "现有LLM智能体在多步规划中依赖大量推理调用，导致高成本和随机性。GATS框架通过UCB1树搜索与三层世界模型（符号匹配、统计学习、LLM预测）相结合，在规划阶段无需任何LLM调用。在含分支路径与死胡同的合成任务和12个挑战场景中，GATS均达到100%成功率，远超LATS（92%/88.9%）和ReAct（64%/23.9%），且生成确定性计划、零方差。这表明系统化搜索结合学习型世界模型可替代LLM引导的探索。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08894v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1484, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08894v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 925, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08894v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1077, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08894v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1279, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08894v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 996, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08894v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1375, \"height\": 775, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08894v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 631, \"height\": 267, \"label\": \"Table\"}]"
motivation: LLM智能体规划时频繁调用LLM导致高计算成本和随机行为，需减少推理依赖。
method: 提出GATS框架，采用UCB1树搜索+三层世界模型（L1符号匹配/L2执行统计/L3 LLM预测），规划零LLM调用。
result: "合成任务成功率100%，12个挑战场景均100%，而LATS为88.9%，ReAct为23.9%。"
conclusion: 系统化搜索与学习型世界模型能显著优于LLM引导的探索，实现高效确定性规划。
---

## 摘要
大语言模型（LLM）智能体在多步规划任务中展现出潜力，但现有方法如LATS（语言智能体树搜索）和ReAct在规划过程中严重依赖LLM推理，导致高计算成本和随机行为。我们提出GATS（图增强树搜索），一种结合基于UCB1的系统树搜索与分层世界模型的规划框架，在推理过程中无需调用LLM即可实现更优的规划性能。我们的三层世界模型整合了：（L1）精确符号动作匹配，（L2）从执行日志中学习的统计信息，以及（L3）针对未知动作的基于LLM的预测。在具有分支路径和死胡同的合成规划任务中，GATS实现了100%的成功率，而LATS为92%，ReAct为64%。在涵盖12个具有挑战性场景（包括编码工作流、网页导航和长期任务）的综合压力测试中，GATS保持100%成功，而LATS降至88.9%，ReAct降至23.9%。GATS在规划过程中每个任务需要零次LLM调用（而LATS每个任务需要37次），并生成确定性计划，运行间方差为零。我们的结果表明，基于所学世界模型的系统搜索在智能体规划方面可以显著优于LLM引导的探索。

## Abstract
Large Language Model (LLM) agents have shown promise in multi-step planning tasks, but existing approaches like LATS (Language Agent Tree Search) and ReAct rely heavily on LLM inference during planning, leading to high computational costs and stochastic behavior. We present \textbf{GATS} (Graph-Augmented Tree Search), a planning framework that combines systematic UCB1-based tree search with a layered world model to eliminate LLM calls during inference while achieving superior planning performance. Our three-layer world model integrates: (L1) exact symbolic action matching, (L2) statistics learned from execution logs, and (L3) LLM-based prediction for unknown actions. On synthetic planning tasks with branching paths and dead-ends, GATS achieves \textbf{100\% success rate} compared to 92 % for LATS and 64\% for ReAct. On a comprehensive stress test spanning 12 challenging scenarios -- including coding workflows, web navigation, and long-horizon tasks -- GATS maintains \textbf{100\% success} while LATS drops to 88.9 % and ReAct to 23.9%. GATS requires \textbf{zero LLM calls per task} during planning (vs. 37 per task for LATS) and produces deterministic plans with zero variance across runs. Our results demonstrate that systematic search with learned world models can substantially outperform LLM-guided exploration for agent planning.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有 LLM 智能体（如 ReAct、LATS）在多步规划任务中严重依赖 LLM 推理调用，导致计算成本高、行为随机（不可复现）。LATS 采用蒙特卡洛树搜索但仍需在每个节点调用 LLM，每任务平均 37 次 LLM 调用，且存在方差。
- **研究动机**：解耦世界模型与 LLM，让规划过程不依赖推理时的 LLM 调用，同时保持甚至提升规划成功率。
- **背景**：经典规划（STRIPS）、强化学习中的世界模型、以及 LLM 作为推理引擎的兴起。作者指出现有 API 基准（如 API-Bank）仅测试单步 API 选择，不足以评估多步规划能力，因此构造了合成任务。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：GATS 采用**分层世界模型**（L1 符号匹配 + L2 统计学习 + L3 LLM 预测）与 **UCB1 树搜索**相结合，在规划阶段完全避免 LLM 调用，仅在一开始的“引导”阶段（bootstrapping）使用 LLM 学习未知动作的效果，之后全部由 L1/L2 覆盖。
- **关键技术细节**：
  - **分层世界模型**：查询时按 L1→L2→L3 顺序，一旦某层置信度高于阈值（τ）则采用。
    - L1（符号层）：已知动作的 STRIPS 式前提-效果匹配，置信度 1.0（适用）或 0.0（不适用）。
    - L2（统计层）：从执行日志中学习的转移统计，返回最频繁的效果，置信度 c = min( count(a)/10, 1.0)。
    - L3（生成层）：对未知动作调用 LLM 预测，置信度固定 0.5，结果缓存。
  - **状态转移图内存**：显式构建有向图 G=(V,E)，相同规范状态合并（类似置换表），搜索统计（访问次数、Q值）跨路径共享，跨步骤持久化。
  - **UCB1 选择**：在给定状态 s 下，每个动作作为臂，选择 UCB(s,a) 最大的动作。公式：
    \[
    a_t = \arg\max_{a\in A_{app}(s)} \left[ Q(s,a) + c\sqrt{\frac{2\ln N(s)}{N(s,a)}} \right]
    \]
    未访问动作默认无穷大，保证所有动作至少尝试一次。
  - **状态价值估计**：基于 BFS 计算到目标的最短距离 d_G(s,G)，价值 = α/(d_G+1)（α=10），不可达则价值=0。
  - **价值备份**：考虑世界模型置信度 p，v = p·StateValue(s') - λ(1-p)（λ≥0 惩罚不确定性）。
  - **算法流程**（Algorithm 2）：
    1. 从初始状态 s0 开始，G = 空图。
    2. 循环（当目标未达成且步数小于最大步数）：
        a. 获取当前状态下适用动作。
        b. 调用 GATS Search（Algorithm 1）在图中执行 UCB1 搜索，预算 b 步。
        c. 选择 Q 值最高的动作 a*，预测下一状态 s'。
        d. 执行 a*，追加到计划 π，s ← s'。
    3. 返回 π 或失败。
- **公式与推导**：文中给出了期望成本公式 E[C_W] = P(L1)c1 + P(L2)c2 + P(L3)c3，以及 LLM 调用数界：|{(k(s),a) : L1失败, L2失败, (k(s),a) ∉ Cache}|。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **数据集/场景**：
  - **合成规划任务**：100 个任务，分为 Easy (20, 3步)、Medium (55, 5步, 资源管理)、Hard (25, 7+步, 多个死胡同)。包含顺序依赖、分支路径、死胡同、资源约束等挑战。
  - **API-Bank Level 1/2**：用于验证与真实 API 格式的兼容性（单步选择）。
  - **压力测试**：12 个类别，每类 10 个任务（共 120 个），包括 coding_task、web_navigation、deep_horizon、critical_choice、no_backtrack、high_branching、resource_puzzle、trap_heavy、deceptive、memory_limit、very_long_horizon、commitment_cascade。步数从 4 到 15 步不等。
- **Benchmark**：无现成多步规划基准，作者自建合成任务和压力测试。
- **对比方法**：
  - Greedy (Oracle)：完美信息下 BFS 选择最高价值状态的动作（上限）。
  - ReAct：LLM 直接选动作，无搜索。
  - LATS：LLM 引导的蒙特卡洛树搜索（动作提议和价值估计），与 GATS 使用相同预算。
- **评价指标**：成功率、最优性（最优长度/实际长度）、LLM 调用次数、方差。

## 4. 资源与算力

- **文中未明确说明 GPU 型号、数量或训练时长**。仅提到 LLM 使用 Llama 3.2 via Ollama 进行推理，但未给出具体硬件配置或训练世界模型的计算开销。论文重点在规划阶段零 LLM 调用，而 bootstrapping 阶段（约 50 次 LLM 调用）被认为是一次性成本，未详细量化。

## 5. 实验数量与充分性

- **实验组数**：
  - 主实验：100 个合成任务，5 个随机种子。
  - 压力测试：12 类别 × 10 任务 × 3 种子 = 360 次运行（但统计时按类别汇总）。
  - 消融实验：
    - 搜索预算消融 (b=1,5,10,20) 对比成功率和节点数。
    - 世界模型层消融：去 L1、去 L3 分别测试，并报告层使用占比。
    - 超参数敏感性：探索常数 c (0.5,1.0,2.0) 下成功率。
- **充分性分析**：实验较为充分。覆盖了不同难度、多种挑战类型、消融分析、预算影响。对比方法控制预算一致。但仅在合成任务上测试，缺乏真实世界 API 基准（如 ToolBench）的评估。实验客观性较好：使用多个种子报告均值与方差，统计显著性通过 McNemar 检验。但压力测试中 GATS 和 LATS 的预算均为 b=20，表中分类对比显示 GATS 全部 100%，LATS 部分类别 100% 但部分较低，差异明显。

## 6. 论文的主要结论与发现

- GATS 在合成规划任务（分支、死胡同）上达到 **100% 成功率**，显著优于 LATS (92%) 和 ReAct (64%)。
- 在 12 类压力测试中 GATS **全部 100%**，而 LATS 平均 88.9%，ReAct 仅 23.9%。
- **零 LLM 调用**：规划阶段每任务 0 次 LLM 调用（LATS 约 37 次），**零方差**（确定性）。
- 世界模型层消融显示：L1 可完全覆盖已知动作；去掉 L1 或 L3 后仍保持 100% 成功率（在已有 L2 统计覆盖时），说明架构稳健。
- 搜索预算消融：b=1（贪婪）成功率 0%，b=5 提升至 84%，b=10 饱和至 100%。
- 核心发现：**系统化 UCB1 搜索 + 学习型世界模型优于随机 LLM 引导的探索**。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 分层世界模型巧妙结合符号、统计、生成三种粒度，优先使用低成本高置信度层，极大减少 LLM 调用。
  - 状态转移图 + 持久化内存：跨路径共享，跨步骤复用，避免重复计算。
  - UCB1 保证探索与利用平衡，适用于不可逆决策场景。
  - 价值估计结合 BFS 最短距离，简单有效。
- **实验设计亮点**：
  - 合成任务系统化覆盖多步规划核心挑战（死胡同、分支、资源、长期依赖）。
  - 压力测试分类细致（12 类），覆盖现实场景（coding、web nav）。
  - 消融实验（预算、层、超参数）完整，统计检验。
  - 对比方法预算对齐，公平比较。

## 8. 不足与局限

- **实验覆盖**：仅使用合成任务和 API-Bank（单步），未在真实多步 API 基准（如 ToolBench）或开放域环境（如 WebArena）上评估，泛化性存疑。
- **偏差风险**：L1 层依赖已知动作规范（STRIPS），在真实场景中可能难以获得完整规范，导致对 L3 依赖增加，效率优势缩小。作者承认这一“设计选择”。
- **应用限制**：BFS 价值估计在动作空间大或深度大时指数复杂度（文中提到 |A|>20, d>10 时 BFS 不实用），虽可替换为学习型价值网络但当前未实现。
- **资源与算力不透明**：bootstrapping 阶段 LLM 调用的具体成本（时间、GPU 型号）未报告，影响可复现性。
- **未讨论失败案例**：GATS 在合成任务 100% 成功，但压力测试中某些 LATS 也达 100% 的类别未说明为何 GATS 更优，缺乏失败模式分析。
- **对比方法限制**：LATS 默认使用 LLM 提议动作，但 GATS 利用已知动作集，不对称性虽被讨论但仍会影响公平性。

（完）
