---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: "AutoSpec: 通过归纳逻辑编程实现LLM智能体的安全规则演化"
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 为LLM代理进化安全规则，涉及代理控制与安全
tldr: "大型语言模型代理在自动化任务时存在安全风险，现有手工规则存在高误报或漏报，神经分类器缺乏可解释性。AutoSpec通过归纳逻辑编程和反例引导的归纳合成，自动从用户标注中演化安全规则，从专家规则开始迭代修正。在代码执行和具身代理领域，F1分别提至0.98和0.93，误报降低94%，在4-5次迭代内收敛。ILP方法比启发式CEGIS的F1高4.8倍，生成的规则可读、可审计且泛化良好。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 559, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: 现有LLM代理安全方法中，手工规则难以平衡精确率和召回率，而神经分类器缺乏可解释性。
method: AutoSpec使用归纳逻辑编程从用户安全/不安全标注中，通过反例引导合成自动演化部署的专家规则。
result: "在291条执行轨迹上，规则F1达0.98和0.93，误报降低94%，收敛于4-5次迭代，F1比启发式CEGIS高4.8倍。"
conclusion: AutoSpec自动生成可解释、可审计的安全规则，有效平衡了安全性与实用性，并泛化到未见场景。
---

## 摘要
大型语言模型（LLM）智能体通过将语言模型与外部工具和环境集成，日益自动化地完成复杂任务。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有的安全方法面临一个根本性的权衡：手工制定的规则可解释但脆弱，过于保守的规则会阻止安全操作（高误报率），而宽松的规则则会遗漏不安全行为（高漏报率）。神经分类器缺乏安全关键部署所需的可解释性。我们提出AutoSpec，这是一个通过反例引导的归纳合成（CEGIS）框架，由归纳逻辑编程（ILP）引导，从用户的安全/不安全注释中自动演化已部署的专家设计安全规则。从专家规则和带注释的轨迹流开始，AutoSpec迭代评估规则，挖掘误报和漏报反例，使用ILP学习哪些谓词可以区分它们，生成候选规则编辑，并验证候选以选择最佳修订。关键洞察在于，ILP能高效识别在漏报中频繁出现但在误报中很少出现的谓词（反之亦然），从而大幅修剪规则编辑的指数级搜索空间。此过程持续直至收敛，产生平衡精确率和召回率的可解释规则。我们在涵盖代码执行和具身智能体领域的291条执行轨迹上评估了AutoSpec。AutoSpec在两个领域上将规则的F1值分别提升至0.98和0.93，在保持高召回率的同时将误报最多减少94%，并在4-5次迭代内收敛。ILP引导的方法相比启发式CEGIS实现了高达4.8倍的F1提升。学习到的规则可读、可审计，并能泛化到未见场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

# 论文《AutoSpec: 通过归纳逻辑编程实现LLM智能体的安全规则演化》详细总结

## 1. 核心问题与整体含义

- **研究动机**：大型语言模型（LLM）智能体在自动化任务时，通过集成外部工具和环境带来显著安全风险（如执行破坏性命令、泄露敏感数据、违反领域约束）。
- **现有方案的局限**：手工制定的安全规则可解释但脆弱，难以平衡精确率和召回率（过于保守会阻挡安全操作，过于宽松则漏报不安全行为）；神经分类器虽能自适应，但缺乏可解释性，不适合安全关键部署。
- **核心问题**：如何自动、持续地演化已部署的专家手工规则，使其从不断累积的用户安全/不安全标注中自适应改进，同时保持规则的可解释性和可审计性。
- **整体含义**：本文提出AutoSpec，将规则维护从手动、一次性编写转变为持续的、标注驱动的演化问题，为LLM智能体的安全防护提供了一种自动、可解释、可审计的进化机制。

## 2. 方法论

- **核心思想**：将规则演化形式化为反例引导的归纳合成（CEGIS）问题，其中用户标注（安全/不安全）作为反例；再利用归纳逻辑编程（ILP）高效识别区分反例的谓词，从而剪枝指数级搜索空间，指导规则编辑。
- **关键技术细节**：
  - **输入**：初始专家规则集R₀、带标注的执行轨迹集Dₗ（每条轨迹标记为安全/不安全）、谓词库P。
  - **迭代循环（Alg. 1）**：
    1. **反例挖掘**：评估当前规则Rᵢ在Dₗ上的表现，收集误报（FP，安全轨迹被错误标记）和漏报（FN，不安全轨迹被漏检），并记录每个反例中触发了哪条规则、哪些谓词失败/成功。
    2. **ILP引导的谓词学习**：将漏报（FN）作为正例、误报（FP）作为负例，使用ILASP求解器，学习一个最简的谓词组合假设H（形如多个谓词合取的析取），使得H覆盖所有正例但不覆盖任何负例。H中的谓词即为区分性建议，同时根据方向（提高召回还是精确）指定编辑操作类型。
    3. **候选规则生成与选择**：基于ILP建议，生成三类候选规则：单步编辑（应用一个建议）、混合修复链（同时提高精度和召回）、失败驱动分支（针对残留FN添加高精度析取分支）。每个候选在Dₗ上评估，选择最大化排序得分S(R') = (F1, 精确率, -未解决反例数, -规则数) 的候选作为Rᵢ₊₁。
    4. **验证与收敛**：若候选得分未提升、达到目标阈值或迭代次数耗尽则停止；否则继续下一轮。算法保证单调得分提升、局部最优，但不保证全局最优。
- **规则编辑操作**（表2）：
  - **AddConjunct**：添加合取谓词（收紧规则，减少FP）
  - **AddException**：添加否定谓词作为例外（收紧规则，减少FP）
  - **AddDisjunct**：添加析取分支（放宽规则，减少FN）
  - **Relax**：删除一个文字（放宽规则，减少FN）

## 3. 实验设计

- **数据集/场景**：
  - **代码执行领域**：191条轨迹（91条不安全来自RedCode-Exec基准[14]，覆盖25个风险类别；100条安全由GPT-5.1-Codex生成，镜像不安全类别但避免触发谓词）。轨迹长度1-9个事件（平均1.4）。
  - **具身智能体领域**：100条轨迹（52%不安全），来自SafeAgentBench[39]，模拟AI2-THOR环境中家用机器人执行任务。轨迹长度1-15个事件（平均5.1）。所有轨迹手动标注为轨迹级标签。
- **基准方法**：
  - B1：专家设计的静态规则（AgentSpec[34]部署的规则）
  - B2：LLM分类器（使用标注作为少数样本，直接分类轨迹是否安全）
  - B3：无ILP的CEGIS（仅基于覆盖率的启发式谓词排序）
  - B4：随机搜索（在固定预算内随机采样候选）
- **评估指标**：轨迹级别的精确率、召回率、F1值；收敛迭代次数；规则复杂度；候选效率。

## 4. 资源与算力

- 文中明确提到：所有实验运行在一台 **MacBook Pro（M2 Max, 32GB RAM）** 上，未使用GPU。
- ILP求解器ILASP v4.4.1使用 **120秒超时** 限制。
- 训练时间：代码执行领域总耗时20.1秒（ILP引导） vs 4.4秒（启发式）；具身智能体领域11.8秒（ILP） vs 4.3秒（启发式）。**未提及GPU型号、数量或分布式训练资源**。

## 5. 实验数量与充分性

- **核心实验组**：
  1. **RQ1（有效性）**：表3，在两个领域对比AutoSpec与B1~B4，报告精确率、召回率、F1及FP/FN数量。
  2. **RQ2（搜索效率）**：图4展示收敛轨迹（F1、精确率、召回率随迭代变化）；表4对比ILP引导与启发式谓词选择（迭代次数、最终F1、候选数、时间）；消融实验表5分析ILP、多步编辑、失败驱动分支贡献。
  3. **RQ3（泛化与可用性）**：图5展示从70%任务学到规则在30%未见任务上的泛化表现；图6展示用户研究（10名从业者，对比AutoSpec规则与LLM分类器在可解释性、可操作性、信任度上的评分）。
  4. **额外分析**：收敛行为、候选效率、规则复杂度等。
- **实验充分性评价**：
  - 覆盖两个差异大的领域（代码执行和具身机器人），增强了结论的稳健性。
  - 基线对比全面：静态规则、LLM分类器、无ILP的CEGIS、随机搜索，覆盖了主要替代方案。
  - 消融实验检验了ILP、多步编辑、失败驱动分支三个关键组件的贡献。
  - 用户研究（10人，120次比较）提供了定性证据支持可解释性和可信度。
  - **总体客观公平**：方法在相同轨迹集上比较，评估指标标准，没有明显偏向。但数据集大小有限（总共291条轨迹），且仅来自两个领域；可能不能代表所有LLM智能体场景。

## 6. 主要结论与发现

- **有效性**：AutoSpec在代码执行领域达到F1=0.98（精确率0.96，召回率1.0），相比专家规则（F1=0.698）提升40.4%；在具身智能体领域达到F1=0.933（精确率0.925，召回率0.942），相比专家规则（F1=0.493）提升89.2%。误报最多降低94%（代码执行从31 FP降至2 FP），保持了高召回率。
- **搜索效率**：收敛于4-5次迭代，ILP引导比无ILP启发式CEGIS的F1高4.8倍（代码执行0.98 vs 0.203）。即使消耗稍多时间（20.1s vs 4.4s），质量提升显著。
- **泛化能力**：从70%任务学习到的规则在30%未见任务上，代码执行F1仅下降1.6%（0.973→0.957），具身智能体下降4.7%（0.889→0.842），表明规则捕捉到了真实不安全模式而非过拟合。
- **可用性**：用户研究显示，从业者对AutoSpec规则在可解释性（4.97 vs 3.45）、可操作性（4.83 vs 2.73）、信任度（4.80 vs 2.77）上显著优于LLM分类器（p<0.01），84.2%的配对比较中更偏好AutoSpec。
- **消融实验**：ILP引导是最关键组件（移除后F1从0.98降至0.20）；多步编辑和失败驱动分支也显著影响效果和效率。

## 7. 优点

- **方法创新性**：首次将LLM智能体安全规则演化形式化为ILP引导的CEGIS问题，利用ILP学习区分性谓词并剪枝搜索空间，思路新颖且实际有效。
- **强可解释性**：输出为基于谓词的符号规则，完全可审计、可追踪，用户可直接理解和修改，适合安全关键场景。
- **自动化程度高**：仅需要用户标注（审批/拒绝的标注）作为输入，无需专家手动调整规则，可适应模型、工具、工作负载的变化。
- **实验设计全面**：覆盖两个代表性领域，多基线对比、消融实验、用户研究，结论支持充分。
- **实际部署友好**：规则执行零推理成本，可与其他防护层（神经护栏、沙箱等）结合；迭代过程可异步进行，不影响在线延迟。
- **泛化能力好**：从有限标注数据学习到的规则能泛化到未见任务，验证了方法的实用性。

## 8. 不足与局限

- **领域覆盖有限**：仅评估了代码执行和具身智能体两个领域，未涉及Web代理、数据库代理、多智能体协作等更复杂场景。
- **数据集规模较小**：总共291条轨迹，每个领域100-191条，可能不足以反映真实生产环境的多样性和长尾不安全行为。
- **依赖谓词库完备性**：规则质量直接取决于预定义的谓词库P是否涵盖区分不安全行为所需的基本特性；若谓词缺失，则无法学到有效规则。文中承认“覆盖缺口”是可测量信号，但未讨论如何自动扩展谓词库。
- **局部最优而非全局最优**：算法只保证局部单调提升，不保证找到全局最优规则集（NP-hard问题）。在实践中可能收敛到次优解。
- **未与最新神经方法充分对比**：仅与简单的LLM分类器（5-shot）比较，未与更先进的细粒度护栏（如Llama Guard、NeMo Guardrails）或微调模型对比，可能低估神经方法在特定条件下的表现。
- **跨领域迁移未研究**：未探讨在一个领域学到的规则是否可迁移到另一个领域，或者如何适应谓词库不同的场景。
- **用户研究规模小**（10名从业者），虽然统计显著，但样本量较小，可能不具广泛代表性。

（完）
