---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 长视界智能体强化学习中的组图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 面向长视野智能体强化学习的组图策略优化，利用图结构状态转移直接处理长视野规划
tldr: "长程智能体强化学习因奖励稀疏延迟且忽略状态转移图结构，导致高方差和局部信用分配。G2PO将线性轨迹转化为全局状态转移图，通过组聚合状态值估计降低方差，并采用边中心优势估计聚焦关键转移。在WebShop、ALFWorld、AppWorld上，G2PO相比GRPO等基线成功率提升高达22.2%。该工作为长程智能体任务提供了基于图结构的组策略优化新范式，显著改善了信用分配。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323}]"
motivation: 现有步级强化学习在长程任务中因线性轨迹视角忽略状态转移图结构，导致高方差状态值估计和局部信用分配。
method: G2PO将交互轨迹转化为全局状态转移图，通过组聚合状态值估计降低方差，并采用边中心优势估计基于全局TD误差聚焦关键转移。
result: "在WebShop、ALFWorld、AppWorld上，G2PO相比GRPO等基线成功率提升最高达22.2%。"
conclusion: G2PO开创了基于图结构的组策略优化，有效解决了长程智能体任务中的信用分配难题。
---

## 摘要
基于组的强化学习显著提升了大语言模型在智能体场景中的表现。为获得更细粒度的策略更新，近期智能体强化学习框架已从轨迹级训练转向步骤级训练。然而，长视界智能体强化学习面临严重的奖励稀疏性和延迟问题，因为反馈往往在数十步交互后才给出。现有步骤级框架虽然细化了训练粒度，但其信用分配仍较为粗糙，且仍将智能体探索视为孤立的线性轨迹。这种过度简化的视角忽略了状态转换固有的图结构，导致状态价值估计方差高且信用分配短视且局部化。为克服这些关键瓶颈，我们提出组图策略优化，这是一种专为多轮智能体任务设计的新型基于组的强化学习算法。该方法将线性交互轨迹显式转化为全局状态转换图。通过聚合不同轨迹中的相同观测，我们引入组聚合状态价值估计，降低了采样方差和轨迹依赖偏差。此外，我们将智能体动作重新定义为状态节点间的转换，并提出基于边的优势估计策略。通过在整个图上全局标准化时序差分误差，该方法能明确识别并优先处理推动绝对任务进展的关键转换。在代表性长视界基准测试（WebShop、ALFWorld和AppWorld）上的大量实验表明，该方法显著优于最先进的基于提示的基线和强化学习基线，相比GRPO取得了高达22.2%的成功率提升。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大语言模型智能体在多轮长视界交互任务（如购物网站导航、室内机器人操作、软件API调用）中面临严重的**奖励稀疏性和延迟**问题——反馈往往在数十步交互后才给出，导致信用分配困难。
- **现有方法的不足**：最新的步骤级训练框架（如GiGPO）虽然细化了训练粒度，但仍将智能体探索视为**孤立的线性轨迹**，忽略状态转移固有的**图结构**。这导致两个关键瓶颈：
  - 状态价值估计方差高：同一状态在不同轨迹中可能通向不同结果，单样本估计不可靠。
  - 局部组比较范围有限：仅在同一状态下比较动作，无法区分“轻微改进”与“关键突破性动作”的全局重要性。
- **本文目标**：提出G2PO算法，将线性轨迹转化为**全局状态转移图**，从根本上解决长视界智能体强化学习中的信用分配问题。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将多条采样轨迹中的相同或相似观测聚合成**状态组节点**，以动作为**边**，构建**状态转移图**。
- 基于图结构设计两种新型机制：**组聚合状态价值估计**和**边中心优势估计**。

### 关键技术细节
1. **状态组图构建**：
   - 对给定任务x，采样N条完整轨迹，将所有中间观测按**相同观测内容**聚类为G个状态组{G_k}。
   - 将状态组作为节点，动作为边，构建图{V, E}。

2. **组聚合状态价值估计**：
   - 先计算每个步骤的折扣回报：v_ij = γ^{T-j+1} R_i（R_i为轨迹最终奖励）。
   - 对每个状态组G_k，取其所有组成步骤的**平均值**作为该组价值：V(G_k) = (1/|G_k|) Σ v_ij。
   - **动机**：平均化来自不同轨迹的相同状态结果，降低采样方差，消除未来步噪声。

3. **多粒度优势估计**（三种优势合并）：
   - **节点中心优势 (A_NC)**：对同一状态组G_k，收集所有可能下一状态组的值作为局部参考集，将当前动作的下一状态值相对于该集标准化。
   - **边中心优势 (A_EC)**：计算每个动作的1步TD误差 δ_ij = V(G_{k'}) - V(G_k)。然后对所有轨迹所有步骤的δ进行**全局标准化**：A_EC = (δ_ij - mean(δ)) / std(δ)。**关键创新**：全局视角能识别绝对贡献大的关键转移。
   - **回合级优势 (A_EP)**：与GRPO类似，基于最终奖励标准化：A_EP = (R_i - mean(R)) / std(R)。
   - **合并优势**：A_ij = A_EP + w·(A_NC + A_EC)，w为平衡权重。

4. **目标函数**：采用GRPO框架的裁剪代理目标，加上KL散度惩罚。

### 算法流程（文字说明）
1. 采样任务x，初始化N个相同环境。
2. 用旧策略π_θold进行多步采样，获得N条完整轨迹及最终奖励。
3. 将中间观测聚类为状态组，构建状态图。
4. 计算每个步骤的折扣回报，组聚合求状态组价值。
5. 计算节点中心优势、边中心优势、回合级优势，合并得到每步优势。
6. 最大化目标函数更新策略。

## 3. 实验设计

### 数据集/场景
- **WebShop**：模拟电商网站，要求智能体搜索、导航、选择商品以满足用户需求。评价指标：得分和成功率。
- **ALFWorld**：基于文本的室内环境，6类家务任务（Pick, Look, Clean, Heat, Cool, Pick2）。评价指标：每类成功率及整体成功率。
- **AppWorld**：通过Python API调用与多个应用交互，要求长时间规划和工具使用。评价指标：成功率和得分。

### 对比方法
- **前沿离线模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。
- **提示式智能体**：ReAct、Reflexion。
- **强化学习方法**：
  - PPO（带critic的标准方法）
  - RLOO（留一法优势）
  - GRPO（组相对策略优化）
  - GiGPO（组内组策略优化，当前步骤级SOTA）
- **本文方法**：G2PO。

### 基础模型
- WebShop和ALFWorld：Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct。
- AppWorld：Qwen2.5-14B-Instruct（因难度较高）。

### 超参数设置
- 组大小N=8（所有组方法一致），学习率1e-6，无效动作惩罚系数0.1，使用vLLM推理引擎。
- G2PO额外参数：γ=0.95，w=1（在消融中调优至0.8最佳）。

## 4. 资源与算力

- **WebShop和ALFWorld**：使用 **4×H100 GPU**，训练 **100次迭代**（iterations）。
- **AppWorld**：使用 **8×H100 GPU**，训练 **50次迭代**。
- 论文指出G2PO在优势计算阶段仅增加约1秒的额外时间（基于CPU，无GPU开销），几乎不增加计算成本。

## 5. 实验数量与充分性

- **主要实验结果**：在WebShop和ALFWorld上，对1.5B和7B模型均报告了平均成功率及标准差（3个随机种子），整体上大幅超越所有基线。
- **AppWorld实验**：在14B模型上报告了成功率和得分，G2PO优于所有方法。
- **消融实验**：逐步添加A_NC、组聚合、A_EC（最终G2PO），在WebShop和ALFWorld上验证每个组件贡献。
- **超参数分析**：对平衡权重w进行调优（图4a），展示性能曲线。
- **方差分析**：展示状态价值方差分布（图4b），论证组聚合必要性。
- **效率分析**：对比不同方法的平均交互步数（图4c），G2PO步数更少。
- **计算时间分析**：分解每训练步时间（图5），G2PO额外耗时仅1秒。
- **组大小动态分析**：展示训练过程中平均组大小变化及分布（附录图6、7）。
- **案例分析**：ALFWorld中一个具体状态转移示例（附录图11），说明边中心优势优势。

### 公平性/客观性评价
- 所有RL方法使用相同超参数和提示格式，组大小统一为8，实验环境一致（vLLM、相同奖励函数）。
- 报告多次运行的平均值和标准差，结果可信。
- 对比了多种类型基线（提示式、RL方法），覆盖面广。

## 6. 主要结论与发现

1. **G2PO在所有三个基准测试上均显著优于所有对比方法**，在WebShop上相对GRPO提升高达22.2%（7B模型），在ALFWorld上提升14.4%。
2. **组聚合状态价值估计**有效降低了状态价值估计方差，这是G2PO性能提升的关键。
3. **边中心优势估计**通过全局标准化TD误差，能识别出真正推动任务进展的关键动作，优于局部组比较。
4. **G2PO在保持计算效率的同时**，仅增加极小额外时间（1秒/训练步），几乎无额外开销。
5. **训练后的模型在任务中交互步数更少**，表明决策效率提升，推理成本降低。

## 7. 优点

- **创新性强**：首次将智能体交互轨迹显式建模为**状态转移图**，从根本上克服了线性轨迹视角的局限性。
- **理论支撑**：附录B提供了方差缩减的数学证明（组聚合降低价值估计方差至σ²/|G_k|；边中心优势方差上界低于或等于轨迹级优势）。
- **工程有效**：组件设计简洁，几乎不增加计算成本；兼容现有GRPO框架，易于集成。
- **实验充分**：在三个具有挑战性的长视界基准上验证，涵盖电商、室内环境、软件接口等不同观测模态；含消融、超参数分析、方差分析、效率分析等。
- **泛化性好**：在AppWorld（复杂API交互）上依然有效，表明方法对不同观测类型（文本描述、API返回）鲁棒。

## 8. 不足与局限

- **实验框架局限性**：仅在verl-agent框架上实现，未在其他智能体RL框架（如RAGEN、Agent Lightning）上验证，可能影响泛化性。
- **采样方式局限**：当前采用同步组采样，未探索树采样或异步采样等更高效的采样方式。
- **状态分组可能过于简单**：完全基于观测内容精确匹配（identical observations），未考虑语义相似但不同的状态（可能丢失信息），对于观测高度变化的环境（如长文本）可能无法有效聚类。
- **超参数w需手动调节**：虽在WebShop上调优至0.8最佳，但未在其他任务上系统调优（论文中固定为1），可能不是最优。
- **奖励函数设计**：使用简单的规则奖励（成功=10，失败=0，无效动作=-0.1），未探索更复杂的奖励塑造，可能限制上限。
- **未在更大模型（如70B）上实验**：目前最大14B，更大规模时图构建的计算开销可能增长。
- **仅评估三个基准**：虽然涵盖不同领域，但未在更多元场景（如多智能体、实时交互游戏）上测试。

（完）
