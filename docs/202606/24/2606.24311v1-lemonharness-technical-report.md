---
title: LemonHarness Technical Report
title_zh: LemonHarness技术报告
authors: "Kailong Ren, Fubo Sun, Jiachen Liu, Liu Yang, Zimo Yin, Jiaying Li, Congli Yin, Ming He, Yu Huo, Jiawei Liu, Zeping Chen, Yubin Huangfu, Ronghua Li, Yixuan Wu, Xing Su, Yanzhi Xu, Likang Wu, Hongke Zhao, Lei Zhang, Xiaohui Geng, Jianping Fan"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24311v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 长程代理执行框架
tldr: "大语言模型代理在长周期任务中频繁修改工作区状态，但由于缺乏显式边界，状态变化难以追踪。LemonHarness通过建立明确的执行边界，将模型调用、工具执行和规则知识纳入统一控制，并使用结构化接口执行状态修改操作。系统引入可复用的规则知识库，将执行规则和验收标准转化为运行时知识。同时，时间感知机制让模型根据剩余时间调整探索与验证策略。在Terminal-Bench 2.0上，LemonHarness配合GPT-5.3-CodeX达到84.49%准确率，更强模型准确率提升至86.52%，表明统一运行时边界、规则知识与时间感知能提高长周期代理执行稳定性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24311v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1228, \"height\": 781, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24311v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1420, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1008, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1013, \"height\": 354, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1009, \"height\": 277, \"label\": \"Table\"}]"
motivation: 长周期代理缺乏执行边界，状态变化分散难跟踪，导致执行不稳定。
method: 通过显式工作区边界、结构化工具接口、可复用规则知识库和时间感知机制统一控制执行。
result: "LemonHarness在Terminal-Bench 2.0上准确率达84.49%-86.52%。"
conclusion: 统一运行时边界、可调用规则知识和时间感知执行能提升长周期代理稳定性。
---

## 摘要
随着大语言模型（LLM）智能体被应用于更长的任务中，它们会在多轮迭代中越来越多地修改工作区状态。然而，智能体通常只观察到工具输出和日志片段，而实际的状态变化发生在文件系统中。没有明确的工作区边界，诸如文件写入和临时工件生成等状态改变操作可能会将更改分散到多个路径。随着时间的推移，这些弱约束的更改不断累积，使得修改文件等状态难以追踪。本文提出了LemonHarness，一个面向长周期智能体的集成执行框架。LemonHarness通过在清晰定义的工作区内约束状态改变操作，并将模型调用、工具执行和规则知识纳入单个受控边界内，从而建立明确的执行边界。状态改变操作，包括文件写入、依赖安装和临时工件创建，都通过结构化工具接口执行，执行反馈作为观察记录，可供后续模型决策使用。系统还引入了可复用的规则知识库，将重复出现的执行规则和验收标准转化为运行时知识。LemonHarness进一步增加了时间感知的执行机制，向模型暴露已用和剩余预算，使其能够随着时间压力的变化重新平衡探索、实施和验证工作，避免因长时间等待或过度验证而超时。在Terminal-Bench 2.0上，LemonHarness_GPT-5.3-CodeX在445次试验中达到了84.49%的准确率；将同一框架与更强的GPT-5.5骨干网络配对，五个任务的平均准确率提高到86.52%。结果表明，统一的运行时边界、可调用的规则知识和时间感知的执行可以提高长周期智能体执行的稳定性。

## Abstract
As large language model (LLM) agents are applied to longer tasks, they increasingly modify workspace state across multiple rounds of iteration. However, agents typically observe only tool outputs and log fragments, while the actual state changes occur in the file system. Without explicit workspace boundaries, state-changing operations such as file writes and temporary artifact generation may scatter changes across paths. Over time, these weakly constrained changes accumulate, making states such as modified files difficult to track. This paper presents LemonHarness, an integrated execution framework for long-horizon agents. LemonHarness establishes an explicit execution boundary by constraining state-changing operations within a clearly defined workspace and bringing model invocation, tool execution, and rule knowledge within a single controlled boundary. State-changing operations, including file writes, dependency installation, and temporary artifact creation, are executed through structured tool interfaces, with execution feedback recorded as observations available to subsequent model decisions. The system also introduces a reusable rule knowledge base, which turns recurring execution rules and acceptance criteria into runtime knowledge. LemonHarness further adds a time-aware execution mechanism that exposes elapsed and remaining budget to the model, so it can rebalance exploration, implementation, and validation effort as time pressure shifts and avoid timeouts from long waits or excessive verification. On Terminal-Bench 2.0, LemonHarness_GPT-5.3-CodeX reached 84.49% accuracy over 445 trials; pairing the same framework with the stronger GPT-5.5 backbone raised the average accuracy to 86.52% across five jobs. The results suggest that a unified runtime boundary, callable rule knowledge, and time-aware execution can improve the stability of long-horizon agent execution.

---

## 论文详细总结（自动生成）

# LemonHarness技术报告 详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）智能体在长周期任务中频繁修改工作区状态（如文件写入、依赖安装、创建临时工件），但由于缺乏显式的执行边界，状态变化分散在文件系统各处，难以追踪，导致“状态漂移”（state drift）。例如，SABER研究表明，突变操作（如文件创建）仅占所有步骤的14-18%，但一次错误的突变操作可使任务成功概率降低57-82%，而非突变操作仅降低7-15%。因此，如何让智能体在长序列执行中保持环境状态的一致性，避免因状态漂移而失败，是重要挑战。
- **整体含义**：LemonHarness通过构建统一运行时边界、可复用规则知识库和时间感知执行机制，将模型推理、工具调用、工作区状态和日志置于同一受控框架内，提升长周期智能体执行的稳定性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将智能体执行的各个组件（系统提示、工具执行器、技能库、执行日志、模型控制循环）统一管理，而不是松散耦合；引入可复用的规则知识作为运行时先验；将时间预算作为运行时状态反馈给模型，使其动态调整探索、实施和验证的精力分配。
- **关键技术细节**：
  - **集成执行框架**：建立明确的执行边界，所有状态改变操作（文件写入、依赖安装、临时目录创建等）都通过结构化工具接口在指定工作区内执行，反馈作为观察记录供后续决策。
  - **可复用规则知识库**：将跨任务可迁移的执行规则和验收标准（如ML工作流的数据分离、重现性要求；生物设计的API稳定性等）转化为运行时先验，在任务开始前注入模型，减少盲目试错。覆盖ML工作流、生物设计、视觉媒体、系统恢复、游戏逻辑等领域。
  - **时间感知执行机制**：
    - 任务开始时分配总时间预算T，根据难度和成本确定。
    - 执行过程中每轮向模型暴露已用时间和剩余时间。
    - 将T划分为四个阶段（默认比例）：探索(0-30%)、实施(30-60%)、验证(60-90%)、保留(90-100%)。
    - 各阶段之间设置5%的宽限带（grace band），避免在边界强制切换切断正在完成的步骤。
    - 阶段转换是单调的（不会回退），最后10%为硬保留区，禁止发起新的状态改变操作。
- **整体流程**：任务输入 → 创建工作区 → 加载系统提示、工具定义、规则知识 → 模型产生动作意图 → 转换为结构化工具调用 → 在工作区内执行 → 返回反馈 → 时间感知信号调整下一阶段行为 → 重复直到任务完成或超时。

## 3. 实验设计：数据集、基准、对比方法

- **数据集/基准**：Terminal-Bench 2.0 和 Terminal-Bench 2.1。Terminal-Bench是一个覆盖真实终端任务场景的基准，每个试验要求智能体根据自然语言指令操作环境、修改文件并达到评估器接受的最终状态。
- **对比方法**：论文主要对比了同一框架在不同骨干模型下的表现（GPT-5.3-CodeX vs GPT-5.5），以及在不同基准版本（2.0 vs 2.1）上的结果。没有与其他独立框架（如ReAct、Toolformer等）进行直接对比，因此对比性较弱。
- **评价指标**：准确率（Accuracy）= 解决的试验数 / 总试验数；还报告了失败数和异常数（主要是超时）。

## 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等具体算力信息。仅提到使用GPT-5.3-CodeX作为主要模型，gemini-3.1-pro-preview作为辅助模型，以及GPT-5.5作为验证模型。这些模型是通过API调用的，本地计算资源未描述。

## 5. 实验数量与充分性

- **实验数量**：
  - Terminal-Bench 2.0：5个job，每个89 trials，共445 trials。LemonHarness_GPT-5.3-CodeX提交了5个job，平均准确率84.49%。
  - 附加验证（GPT-5.5）：5个job共445 trials，平均准确率86.52%。
  - Terminal-Bench 2.1补充验证：最新3个job共267 trials，平均准确率91.76%。
- **充分性分析**：
  - 覆盖了多次独立运行（多个job），每个job包含89个不同任务，且基准版本有更新，实验结果具有可重复性。
  - 没有进行消融实验（如移除规则知识、去掉时间感知等），因此无法量化各组件的单独贡献。
  - 没有与其他长周期智能体框架（如Voyager、ExpeL等）在同一基准上对比，公平性无法充分评估。
  - 实验仅在Terminal-Bench上评估，缺乏在其他基准（如SWE-bench、WebArena等）上的泛化性验证。
  - 总体而言，实验在单一基准上进行了多次验证，但消融和对比不足，充分性有限。

## 6. 论文的主要结论与发现

- 统一运行时边界、可调用规则知识和时间感知执行能够显著提高长周期智能体的执行稳定性。在Terminal-Bench 2.0上，LemonHarness配合中等模型（GPT-5.3-CodeX）达到84.49%准确率，配合更强模型（GPT-5.5）达到86.52%准确率，且异常数减少。
- 在更新版本的基准（2.1）上，平均准确率进一步提升至91.76%，说明框架对基准改进具有鲁棒性。
- 剩余失败主要集中在需要长编译、训练、多模态处理、跨环境适应等任务，这些任务需要更长的执行链和更严格的中间验证。
- 时间感知机制有效避免了因漫无目的的探索或最后时刻的过度验证导致的超时。

## 7. 优点：方法或实验设计上的亮点

- **方法层面**：
  - 集成执行边界的设计简洁直观，解决了状态漂移这一长周期任务核心痛点。
  - 可复用规则知识库将通用领域知识转化为先验，减少模型试错，提高早期方向感。
  - 时间感知执行机制引入阶段性策略，并通过宽限带处理边界平滑，实用性强。
- **实验层面**：
  - 在Terminal-Bench 2.0上进行了多次独立运行（5个job），报告了平均值和波动范围，展示了框架的稳定性。
  - 验证了框架在不同骨干模型（GPT-5.3 vs GPT-5.5）上的表现，表明框架可迁移性。
  - 补充了Terminal-Bench 2.1的验证，展示了框架随基准演进的适应性。
  - 异常分类（超时为主）为后续改进提供了清晰方向。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅在Terminal-Bench单一基准上评估，缺乏在其他代理基准（如SWE-bench、WebArena、GAIA等）上的验证，泛化性未知。
  - 未进行消融实验，无定量证据证明每个组件（集成边界、规则知识、时间感知）的独立贡献。
  - 没有与现有方法（如ReAct、Voyager、ExpeL等）在同一设置下公平对比。
- **偏差风险**：
  - 作者来自联想CTO及合作高校，可能存在利益相关偏差。
  - 规则知识库的内容由作者设计，其覆盖面和质量可能带有主观性。
- **应用限制**：
  - 当前实现与GPT系列模型紧密耦合，对不同工具调用格式、长上下文保持能力、指令遵循能力的差异处理未验证，迁移到其他模型族（如开源模型）的通用性未知。
  - 时间感知的默认参数（阶段比例、宽限带）可能针对Terminal-Bench调优，在其他任务上需要重新设定。
- **资源信息缺失**：未报告计算成本（API调用次数、token消耗、运行时间等），难以评估实际效率。

（完）
