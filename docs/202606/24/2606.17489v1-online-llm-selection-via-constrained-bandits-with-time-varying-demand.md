---
title: Online LLM Selection via Constrained Bandits with Time-Varying Demand
title_zh: 基于时变需求的约束赌博机在线LLM选择
authors: "Yin Huang, Qingsong Liu, Jie Xu"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17489v1"
tags: ["query:moe-routing"]
score: 9.0
evidence: 通过约束赌博机在线选择LLM，时变需求，直接针对模型路由
tldr: 大型语言模型（LLM）在边缘云推理系统中需处理多样任务，其准确性、延迟和成本各异，且任务需求随时间变化。固定选择策略无法满足动态约束。作者将LLM选择问题建模为带硬性预算和软性延迟约束的随机bandit，提出一种在线学习算法，利用置信区间和需求预测来平衡奖励与长期约束。理论证明算法具有次线性遗憾和次线性约束违反，实验表明在动态环境中效果稳健。该研究为资源受限下的在线模型选择提供了理论保障和实用方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17489v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 463, \"height\": 453}, {\"url\": \"assets/figures/arxiv/2606.17489v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1768, \"height\": 454}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17489v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 960, \"height\": 228}]"
motivation: 现有静态LLM选择策略无法适应异构性能、时变需求及多重约束，亟需在线自适应方法。
method: 将问题建模为约束随机bandit，设计基于置信界和需求预测的在线算法，平衡奖励与硬/软约束。
result: 理论证明算法达到次线性遗憾和约束违反，实验验证其在动态资源受限环境中的鲁棒性。
conclusion: 本文为在线LLM选择提供了理论框架和实用算法，有效应对时变需求和多重约束。
---

## 摘要
大语言模型（LLMs）越来越多地部署在边缘云推理系统中，以处理具有异构准确性、延迟和成本配置的多样用户任务。为每个传入任务选择合适的LLM对于确保服务质量和高效资源利用至关重要。然而，模型异构性、随机且未知的性能特征以及时变的任务需求使得静态选择策略不再适用。实际部署中通常施加硬资源预算（如货币支出限制）以及软服务等级要求（如延迟保证）。这些约束给在线决策带来了额外挑战。我们将该问题形式化为一个约束随机赌博机学习任务，其中学习器在包装型（硬）约束和覆盖型（软）约束下顺序选择模型，同时适应时变的任务需求。学习器在无法访问底层奖励、成本或延迟分布的情况下运行，必须依赖部分反馈。我们开发了一种新颖的在线学习算法，利用置信界估计和需求预测来平衡奖励最大化与长期约束满足。我们提供了理论保证，与具有完全信息的离线基准相比，该算法实现了次线性遗憾和次线性覆盖约束违反。在合成工作负载上的实验结果证明了我们的方法在动态、资源受限环境中的有效性和鲁棒性。

## Abstract
Large Language Models (LLMs) are increasingly deployed in edge-cloud inference systems to handle diverse user tasks with heterogeneous accuracy, latency, and cost profiles. Selecting the appropriate LLM for each incoming task is critical for ensuring service quality and efficient resource utilization. However, model heterogeneity, stochastic and unknown performance characteristics, and time-varying task demands make static selection strategies inadequate. Real-world deployments often impose hard resource budgets such as monetary expenditure limits, along with soft service-level requirements such as latency guarantees. These constraints introduce additional challenges for online decision-making. We formulate this problem as a constrained stochastic bandit learning task, where the learner sequentially selects models under both packing-type (hard) and covering-type (soft) constraints, while adapting to time-varying task demand. The learner operates without access to the underlying reward, cost, or latency distributions and must rely on partial feedback. We develop a novel online learning algorithm that leverages confidence-bound estimates and demand predictions to balance reward maximization with long-term constraint satisfaction. We provide theoretical guarantees showing sublinear regret and sublinear covering constraint violations compared to an offline benchmark with full information. Experimental results on synthetic workloads demonstrate the effectiveness and robustness of our approach in dynamic, resource-constrained environments.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大语言模型（LLM）在边缘-云推理系统中处理多样化任务，不同LLM在准确性、延迟和成本上差异显著，且任务需求随时间动态变化。
- **问题**：静态选择策略无法适应模型异构性、随机未知的性能以及时变负载；实际部署还面临硬性货币预算（包装型约束）和软性延迟SLA（覆盖型约束）的双重限制。
- **意义**：需要一种在线决策框架，在无法预知奖励、成本或延迟分布的前提下，同时兼顾奖励最大化与长期约束满足，为资源受限的动态环境提供理论保障和实用算法。

## 2. 方法论：核心思想、技术与算法流程

- **问题建模**：将在线LLM选择形式化为一个约束随机多臂赌博机问题，每个动作（选择某LLM）产生随机奖励、随机成本向量（含包装型与覆盖型约束），任务量qt随时间变化且不可提前知晓。
- **核心思想**：采用**置信界估计**（UCB用于奖励和覆盖约束，LCB用于包装约束）平衡探索与利用；结合**需求预测**（黑盒时序预测器估计总任务量）辅助资源定价；通过**在线梯度下降更新对偶变量**（拉格朗日乘子）动态调整资源偏好。
- **关键技术细节**：
  - 置信半径函数：`rad(v, N, δ) = sqrt(2v log(1/δ)/N) + 4 log(1/δ)/N`
  - 构造UCB_r,t(a)（奖励上界）、LCB_c,t(a)（包装成本下界）、UCB_c,t(a)（覆盖成本上界）。
  - 动作选择规则（公式7）：`at = argmax_a [UCB_r,t(a) - (λ_t^T × Q̂_t / B) · z_conf_t(a)]`，其中z_conf_t(a)根据约束类型统一为向量（包装用LCB、覆盖用负的UCB）。
  - 对偶变量更新：`λ_{t+1} = Π_S(λ_t - η_t ∇ f_t(λ_t))`，目标函数`f_t(λ)`包含归一化后的资源残差。
  - 需求预测：采用“2的幂次”更新策略，仅在t为2的幂时更新总任务量估计Q̂_t，其余时刻复用旧值。
- **算法流程**（COPAC-UCB）：
  1. 初始化λ_1=1_d，设置步长等超参数。
  2. 对每轮t：
     - 预测Q̂_t。
     - 计算所有动作的置信界。
     - 根据(7)选择动作a_t。
     - 若某包装约束累计成本超预算，则停止并执行空动作。
     - 观察反馈，更新剩余预算，更新对偶变量λ_t。
- **理论保证**：证明算法相对于离线最优线性规划基准的遗憾（Regret）和覆盖约束违反均为次线性（O(T^{3/4})项），并受预测误差ε_t影响。

## 3. 实验设计

- **数据集/场景**：基于真实边缘测量报告[9]构建的跟踪驱动模拟器，使用HumanEval编程任务作为工作负载。四种LLM选项：Gemma2_2b, Llama3.2_1b, Qwen2.5_0.5b, Qwen2.5_1.5b，包含准确率、延迟、成本（美元/千tokens）、平均响应长度等异质参数（表1）。
- **约束设置**：硬性总预算$8000；软性80%的任务延迟≤180秒。
- **需求模式**：两种动态序列：
  - i.i.d.高斯分布（均值2，方差0.5）。
  - AR(1)自回归过程（α=2，β=0.5）。
- **对比方法**：扩展后的三种baseline（均无需求预测）：
  - AD-UCB：基于乐观凸优化的BwC算法。
  - PD-BwK：基于乘性更新的原始-对偶BwK算法。
  - SW-UCB：滑动窗口非平稳BwK算法。
- **评估指标**：累积遗憾（相对于离线最优线性规划）、约束违反（SLA覆盖缺失比例）、需求估计误差|Q̂_t - Q|。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长等硬件配置。实验为模拟仿真，无大规模模型训练环节，算力需求较低。

## 5. 实验数量与充分性

- **实验组数**：两组主要实验（i.i.d.和AR(1)），每组展示了三个子图（估计误差、累积遗憾、约束违反）。无单独的消融实验或敏感性分析。
- **充分性**：实验覆盖了两种典型需求模式并对比了三个代表性baseline，证明了算法在平稳和时序相关场景下的优势。但未进行以下扩展：
  - 不同预算/延迟阈值的影响。
  - 更复杂的约束组合（如多种资源）。
  - 预测误差鲁棒性测试（如故意引入偏差）。
- **客观性**：baseline均为领域内知名方法，实验在相同条件下运行，结果呈现清晰，结论可信。

## 6. 主要结论与发现

- COPAC-UCB在两种需求模式下均实现**最低累积遗憾**和**最小约束违反**（甚至轻微超额完成SLA）。
- i.i.d.场景下所有算法性能更优，且COPAC-UCB优势明显；AR(1)场景下由于预测误差传播，所有算法遗憾和违反增加，但COPAC-UCB仍保持领先。
- 需求预测（虽然累加估计）有效帮助算法适应时变负载，尤其对覆盖约束的满足有正面作用。
- 理论边界（次线性遗憾与违反）与实验趋势一致，验证了方法的有效性与稳健性。

## 7. 优点

- **问题新颖性**：首次将LLM选择同时建模为**包装型+覆盖型**约束，并考虑**时变需求**，贴近实际边缘部署场景。
- **理论贡献**：给出次线性遗憾和约束违反的高概率上界，将预测误差纳入分析，理论扎实。
- **算法设计简洁有效**：结合UCB/LCB、对偶变量、需求预测，可解释性强，易于实现。
- **实验真实性强**：基于实际测量数据构建模型参数，非纯合成，提升结果可信度。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了4种LLM和两组需求序列，未探索更多模型、更大规模或更复杂的约束（如多种SLA共存）。
- **未做预测误差消融**：预测准确性对算法性能的影响未量化（仅理论提及），缺少对预测器类型或精度的敏感性分析。
- **未讨论计算开销**：每轮置信界计算和对偶更新带来的额外耗时未评估，实际大规模部署中可能影响实时性。
- **局限性**：假设任务量qt有已知上界q̄，且预测器为黑盒，实际中若预测出现重大偏差，算法稳健性未验证。

（完）
