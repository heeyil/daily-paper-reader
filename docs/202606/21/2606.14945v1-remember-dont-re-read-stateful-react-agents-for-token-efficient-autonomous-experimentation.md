---
title: "Remember, Don't Re-read: Stateful ReAct Agents for Token-Efficient Autonomous Experimentation"
title_zh: 记住，不要重读：用于令牌高效自主实验的有状态ReAct代理
authors: Faramarz Jabbarvaziri
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14945v1"
tags: ["query:agent-lsh"]
score: 8.0
evidence: 有状态ReAct agent实现高效的自主实验规划
tldr: "现有自主实验模式中，大语言模型（LLM）每次迭代都从头重建实验上下文，导致token成本呈二次增长。本文提出有状态ReAct智能体，利用LangGraph将实验历史持久化为类型化状态，通过工具调用接口传递，使每次迭代开销降至常数。在超参数调优和代码优化任务上，token消耗分别减少90%和52%，且优化质量相当。该方法为构建高效的自主实验工作流提供了可复现架构。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14945v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 606, \"height\": 992, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14945v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 1037, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14945v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1449, \"height\": 1021, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14945v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1660, \"height\": 805, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14945v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1660, \"height\": 165, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14945v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1147, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14945v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1108, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14945v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1518, \"height\": 201, \"label\": \"Table\"}]"
motivation: 无状态自主实验每次迭代重读全部历史，token成本呈O(n²)增长，亟需降低开销。
method: 设计基于LangGraph的有状态ReAct智能体，用持久状态携带实验历史，以固定窗口实现O(1) token成本。
result: "超参数调优节省90% token（2,492 vs 24,465），代码优化节省52% token（627K vs 1,275K），质量持平。"
conclusion: 有状态智能体通过结构性消除重复读取历史，显著降低token消耗且不影响优化效果。
---

## 摘要
自动研究模式通过让大语言模型（LLM）迭代修改代码以优化目标指标，实现自主实验。然而，其无状态设计每次迭代都从头重建实验上下文，导致每次迭代的令牌成本为O(n)，总成本为O(n^2)。本文使用LangGraph将该模式重新构建为有状态ReAct代理，其中类型化持久状态通过工具调用接口跨迭代携带实验历史。评估了两个基准：超参数调优（15次迭代，每次迭代观测值小）和代码性能优化（40次迭代，每次迭代观测值大，包含完整源代码和基准结果）。在超参数调优中，有状态代理消耗的令牌减少90%（2,492 vs. 24,465）。在代码优化中，有状态代理消耗的令牌减少52%（627K vs. 1,275K），同时两项任务的优化质量相当。令牌减少是结构性的：无状态代理每次迭代以O(n)成本重读完整历史，而有状态代理在固定大小的对话窗口内以O(1)成本运行。本文详细描述了架构，使从业者能够为自己的工作流程实现有状态自动研究代理。

## Abstract
The autoresearch pattern enables autonomous experimentation by having a large language model (LLM) iteratively modify code to optimize a target metric. Its stateless design, however, reconstructs experimental context from scratch at every iteration, incurring $O(n)$ token cost per iteration and $O(n^{2})$ total. This work reformulates the pattern as a stateful ReAct agent using LangGraph, where typed persistent state carries experimental history across iterations via a tool-calling interface. Two benchmarks are evaluated: hyperparameter tuning (15 iterations, small per-iteration observations) and code performance optimization (40 iterations, large per-iteration observations containing full source code and benchmark results). On hyperparameter tuning, the stateful agent consumes 90\% fewer tokens (2{,}492 vs.\ 24{,}465). On code optimization, the stateful agent consumes 52\% fewer tokens (627K vs.\ 1{,}275K) while achieving comparable optimization quality on both tasks. The token reduction is structural: the stateless agent re-reads the full history at $O(n)$ cost per iteration, while the stateful agent operates within a fixed-size conversation window at $O(1)$ cost. This paper describes the architecture in sufficient detail for practitioners to implement a stateful autoresearch agent for their own workflows.

---

## 论文详细总结（自动生成）

# 论文总结：Remember, Don't Re-read: Stateful ReAct Agents for Token-Efficient Autonomous Experimentation

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：机器学习研究本质上是迭代的——研究者提出假设、修改代码、运行实验、分析结果、决定下一步。虽然单个实验的计算成本已下降，但实验间的人类推理仍为主要瓶颈。Karpathy（2026）提出的“autoresearch”模式让LLM通过迭代修改代码自动完成实验闭环，但该模式采用无状态设计：每次迭代LLM必须重新读取完整的实验结果历史（存储在program.md中），导致每轮输入提示词线性增长，总token成本呈O(n²)增长。
- **核心问题**：这种结构性的低效使得长序列实验变得昂贵且可能触发上下文窗口限制。Huang等（2025）指出LLM无法跨交互维持内部瞬态状态，因此外部状态管理是架构上的必要。
- **整体含义**：本文通过将autoresearch模式重构为有状态ReAct代理，将每次迭代的token成本从O(n)降至O(1)，在保持优化质量的同时显著降低token消耗，为构建可扩展的自主实验工作流提供了可复现的架构参考。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：使用LangGraph构建有状态ReAct代理，将实验历史、策略推理、收敛跟踪等持久化为类型化状态，通过工具调用接口跨迭代传递，而非在每个LLM调用中重新发送完整历史。
- **关键技术细节**：
  - **架构**：采用LangGraph状态图，包含三种节点：REASON（LLM生成推理和工具调用）、TOOLS（执行工具调用并返回观测）、CHECK（评估收敛条件，无LLM参与）。图结构通过条件边路由。
  - **状态模式**：定义`AgentState`类型字典，包含`messages`（消息列表）、`experiment_history`（实验历史列表）、`current_best`（当前最佳结果字典）、`iteration`（迭代次数）、`train_py_content`（训练脚本内容）、`status`（状态字符串）。消息窗口通过滑动窗口（最近20条消息）保持固定大小。
  - **工具**：两个核心工具：`get_experiment_history()`（查询历史）、`run_experiment()`（运行实验并返回指标）。生产变体增加更多工具如`modify_train_py`（含保护措施）、`submit_training_job`等。
  - **收敛条件**：三种情况之一触发终止：目标指标超过阈值、达到迭代预算、LLM认为进一步改进不可能。
  - **有状态摘要机制**：`ExperimentState`类的`summary()`方法只向LLM提供最近5次实验信息和聚合统计（如最佳F1），保证每次迭代输入成本固定为O(1)，完整历史保留在状态中但不进入提示词。

## 3. 实验设计

- **数据集/场景**：
  - **任务1：超参数调优** - 使用UCI Covertype数据集（7类森林覆盖分类，54个特征，子采样至20,000实例），优化XGBoost超参数。每次迭代观测值较小（约200 tokens）。
  - **任务2：代码性能优化** - 优化一个故意低效的Python数据处理函数（约100行，处理10,000条员工记录），包含冒泡排序、字符串循环拼接等反模式。每次迭代观测值较大（完整源代码约1,500-3,000 tokens）。
- **Benchmark**：无公开标准基准，比较同一任务下无状态（stateless）与有状态（stateful）两种架构的性能。
- **对比方法**：无状态runner（每次迭代构造包含完整历史的提示词） vs. 有状态runner（使用持久状态和固定大小窗口）。两种方法均使用Claude Haiku 4.5。

## 4. 资源与算力

- 文中未明确提及使用的GPU型号、数量或训练时长。实验运行在Databricks Serverless Compute上。对于超参数调优，有状态代理平均耗时373秒（无状态116秒，约慢3.2倍），但作者指出多轮对话开销可在更长的实验序列中摊销。

## 5. 实验数量与充分性

- **实验数量**：每个任务运行3个随机种子（42, 123, 456）。超参数调优15次迭代，代码优化40次迭代。还进行了生产代理架构的描述（未进行系统比较实验）。
- **充分性评估**：
  - 优点：包含了两种不同观测规模的任务，展示了token节省差异。使用3个随机种子提供基本统计。
  - 不足：仅两个任务，统计功效有限（3个种子）。未进行消融研究（如改变滑动窗口大小、不同收敛策略）。未在更复杂场景（如提示优化、科学公式发现、硬件设计）验证。仅使用单一LLM（Haiku），未测试更大模型是否能部分弥补无状态设计的不足。未对比其他有状态方法（如MemGPT、Reflexion）。优化质量的差异被指出在噪声范围内，不应视为质量优势。

## 6. 主要结论与发现

- 有状态ReAct代理在两种任务上均实现显著token节省：超参数调优减少90% token（2,492 vs. 24,465），代码优化减少52% token（627K vs. 1,275K）。
- 优化质量相当（超参数调优F1：0.779 vs. 0.764；代码优化加速比：1.91× vs. 1.87×），差异在种子方差范围内。
- token节省是结构性的：无状态每次迭代成本O(n)，有状态固定为O(1)。随着迭代次数增加，比率持续扩大（预期100次迭代时可达4×，200次超过7×）。
- 有状态代理可以运行任意长序列而不触发提示截断或摘要，保留了决策能力。

## 7. 优点

- **结构性效率**：从根本上消除了重复读取完整历史的需求，token成本从O(n²)降至O(n)，具有可扩展性。
- **实现清晰**：提供了足够的架构细节（状态定义、图结构、工具、示例代码），从业者可直接复制。
- **实验设计合理**：选取两种观测规模的任务，展示了不同场景下的token节省比率差异，有助于理解方法适用范围。
- **方法简单有效**：不依赖复杂的内存管理（如MemGPT的分层），而是通过固定窗口+状态存储实现，易于理解和部署。
- **保护机制**：生产版本中`modify_train_py`工具包含保护措施（强制保留评估和日志调用），防止代理破坏评估管线。

## 8. 不足与局限

- **实验覆盖不足**：仅两个任务，缺乏多样性（如提示优化、科学发现、硬件设计等）。统计功效低（3个种子），未进行消融研究（如窗口大小的影响、不同收敛策略）。
- **单一LLM**：仅用Claude Haiku 4.5，未测试GPT-4或其他模型；更大模型可能通过更强的上下文学习部分弥补无状态设计的缺陷。
- **时间开销**：ReAct模式的多次工具调用导致有状态代理在任务1上慢3.2倍（116s vs 373s），对快速训练循环的任务可能不利。
- **未见生产环境大规模验证**：生产代理架构虽有描述，但未提供实际运行结果或与基准的对比。
- **优化质量对比前提不足**：作者明确指出质量差异在噪声范围内，因此无法断言有状态设计不降低质量，但同时也未证明其等价性——需要更多种子或更严格统计检验。
- **忽略其他节省策略**：未讨论或对比其他token优化方法（如历史摘要、压缩、检索增强等），仅聚焦于状态持久化。
- **缺失成本效益分析**：token节省的绝对成本差异在Haiku定价下较小（超参数调优约$0.01 vs $0.02），未讨论其他模型定价下是否还显著。

（完）
