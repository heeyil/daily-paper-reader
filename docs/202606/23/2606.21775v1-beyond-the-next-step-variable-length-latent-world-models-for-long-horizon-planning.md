---
title: "Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning"
title_zh: 超越下一步：面向长程规划的可变长度隐式世界模型
authors: "Tianqi Du, Qi Zhang, Yifei Wang, Yisen Wang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21775v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 变长潜世界模型用于长程规划
tldr: "现有潜在世界模型通常只进行单步预测，在长程规划时递归展开导致误差累积。本文提出可变长度潜在世界模型（VLWMs），直接学习预测动作序列条件下的未来状态，并采用课程训练逐步扩展预测长度。在长程控制任务上，VLWMs平均提升13%，尤其在高难度长程规划任务上表现显著。该方法为改进潜在世界模型的长期预测与规划提供了简单有效的新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1350, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1329, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21775v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1161, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1019, \"height\": 224, \"label\": \"Table\"}]"
motivation: 单步预测递归展开导致长程规划误差累积，训练目标与下游任务不匹配。
method: 提出VLWMs学习预测可变长度动作序列的未来状态，并采用课程训练策略逐步扩展预测长度。
result: "在长程控制任务上平均改进13%，显著超越SOTA LeWM。"
conclusion: VLWMs提供了一种简单有效的方法来提升潜在世界模型的长程预测与规划能力。
---

## 摘要
近年来，世界模型通过学习基于观测和动作预测未来环境状态的概率模型，成为了构建智能代理的一种有前景的范式。特别是，JEPA风格的隐式世界模型通过在紧凑表示空间中学习动作条件动力学，提供了像素空间预测的高效替代方案。然而，现有的隐式世界模型通常依赖于单步预测，并且必须递归地展开以进行长程规划，这导致误差累积以及训练目标与下游规划任务之间的不匹配。为了解决这一局限，我们提出了可变长度隐式世界模型（VLWMs），这是一个学习基于可变长度动作序列预测未来隐式状态的框架。VLWMs不仅训练单步转移，而是直接建模时间延展动力学，使得同一预测器能够评估不同视野下的动作计划。我们进一步引入了一种课程训练策略，逐步扩展动作视野，从短程动力学稳定优化到长程预测。在测试时，我们设计了专为VLWMs量身定制的规划方法，以更好地利用其变长预测能力。长程控制任务的实验表明，VLWMs显著改进了隐式空间世界模型，在不同数据集上相比最先进的LeWM平均提升了13%，特别是在需要扩展规划的任务上取得了更大增益。这些结果表明，VLWM为改进隐式世界模型中的长程预测和规划提供了一种简单而有效的范式。

## Abstract
Recently, world models have emerged as a promising paradigm for building intelligent agents by learning predictive models that estimate future environment states conditioned on observations and actions. In particular, JEPA-style latent world models provide an efficient alternative to pixel space prediction by learning action-conditioned dynamics in compact representation spaces. However, existing latent world models typically rely on one-step prediction and must be recursively rolled out for long-horizon planning, which leads to compounding errors and a mismatch between training objectives and downstream planning tasks. To address this limitation, we propose Variable-length Latent World Models (VLWMs), a framework that learns to predict future latent states conditioned on action sequences of variable lengths. Instead of training only on one-step transitions, VLWMs directly model temporally extended dynamics, allowing the same predictor to evaluate action plans over different horizons. We further introduce a curriculum training strategy that progressively expands the action horizon, stabilizing optimization from short-range dynamics to long-range prediction. At test time, we design planning methods tailored to VLWMs to better exploit their variable-length predictive capabilities. Experiments on long-horizon control tasks show that VLWMs significantly improve latent space world models, achieving 13\% average improvement over the state-of-the-art LeWM across different datasets, with especially large gains on tasks requiring extended planning. These results suggest that VLWM provides a simple yet effective paradigm for improving long-horizon prediction and planning in latent world models.

---

## 论文详细总结（自动生成）

# 论文总结：Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning

## 1. 核心问题与整体含义（研究动机与背景）
- **核心问题**：现有隐式世界模型（如 JEPA 风格、DINO-WM）通常只进行单步预测（t→t+1），在长程规划时需递归展开，导致误差累积（compounding error），且训练目标（单步监督）与下游长程规划任务不匹配。
- **研究动机**：视频帧之间存在大量时间冗余，单步预测容易陷入短程插值或近恒等映射，无法学习高层动力学；长程规划需要模型具备多步语义一致性。
- **整体含义**：作者提出将固定步预测扩展为可变长度预测，使单个模型同时支持短程和长程预测，从而缓解误差累积，提升长程规划性能。

## 2. 论文提出的方法论
### 核心思想
- 训练一个预测器，能够基于当前隐状态和 **可变长度** 的动作序列（长度为 k），直接预测 k 步后的隐状态（t→t+k），而非仅预测下一步。
- 通过课程训练逐步增大 k，稳定优化。
- 在规划时，将长程规划分解为若干可变长度的“跳步”（chunked variable-length latent jumps），减少递归步骤。

### 关键技术细节
- **动作作为 Token（Action-as-Token）**：将每个动作嵌入作为独立的 Transformer token，与状态 token 组成序列输入因果 Transformer，输出位置对应最后 token 的预测隐状态。无需固定动作条件接口，天然支持任意长度动作序列。
- **训练目标**：  
  `$L_{VLWM} = \mathbb{E}_{t, k \sim p(k)} \left\| g_\phi(s_t, a_{t:t+k-1}) - \text{sg}(\bar{f}_{\bar{\theta}}(o_{t+k})) \right\|_2^2$`  
  其中 p(k) 为课程分布。
- **课程训练策略**：将训练分为 K_max 个阶段，第 j 阶段从 {1,...,j} 均匀采样 k。从单步开始，逐步引入更长的预测范围。
- **规划策略（P1/P2/P3）**：
  - P1（固定但任务自适应）：使用固定块大小 k*（如3或5）。
  - P2（随机）：每一步从 {1..K_max} 均匀采样，约束总步数。
  - P3（长到短）：按位置依赖的分布，先大块后小块，实现粗到细。

## 3. 实验设计
### 数据集/环境
- **PushT**：2D 连续操纵任务，推动 T 形块到目标位置。20,000 专家轨迹，平均长度 196 步。
- **OGBench-Cube**：3D 机械臂抓取放置任务。10,000 轨迹，每段 200 步。
- **TwoRoom**：2D 导航任务，两个房间由门连接。10,000 轨迹，平均长度 92 步。

### 基准（Benchmark）
- 每个任务采用 **目标条件规划（goal-conditioned planning）**，目标偏移 ∆ ∈ {25,50,75,100}（对应短/中/长程规划）。
- 规划器使用交叉熵方法（CEM），在隐空间内优化。
- 对比基线：**LeWM**（LeWorldModel，当前 SOTA 隐式世界模型），以及与 LeWM 相同设置下的固定步版本。

### 对比方法
- 主要对比：VLWM（最佳规划器结果） vs LeWM。
- 消融实验：对比有无课程训练、不同规划策略（P1/P2/P3）。
- 额外分析：线性探测隐空间表示质量。

## 4. 资源与算力
- **文中未明确说明**：未提及使用的 GPU 型号、数量、训练时长等具体算力信息。仅给出训练超参数（如 batch size=128, AdamW, 10 epochs）。因此无法评估计算开销。

## 5. 实验数量与充分性
- **实验数量**：较为充分，涵盖三个不同环境、四种目标偏移、三种规划策略、课程消融，以及线性探测分析。
- **充分性与公平性**：
  - 与 LeWM 保持相同编码器、预测器、优化器、CEM 参数，仅改动训练目标和动作输入方式，对比公平。
  - 消融实验验证了课程训练的必要性。
  - 线性探测使用了冻结编码器，对比公平。
  - 缺点是未与其他变长模型（如 HWM）进行直接对比，仅与 LeWM 对比；且未在更复杂（如视觉多样）环境验证。

## 6. 主要结论与发现
- VLWM 在所有数据集和所有目标偏移上超越 LeWM，**平均提升 13%**，长程规划（∆=75/100）提升最显著（如 TwoRoom-100 从 36% 到 68%）。
- 课程训练对长程规划至关重要，去除后性能下降。
- 不同规划策略各有优劣，无通用最优方案；但所有变长策略均优于固定单步。
- 线性探测显示 VLWM 的隐空间能更准确编码物理量（尤其旋转角度），表明变长监督有助于学习语义结构。

## 7. 优点
- **简单有效**：无需复杂架构改动，仅改变训练目标（变长监督）和动作输入方式，就显著提升长程规划。
- **统一框架**：将短程和长程预测统一在单目标下，现有方法可视为特例（k=1）。
- **课程训练**：稳定优化，使模型逐步适应更长预测。
- **动作作为 Token**：架构无关，灵活支持任意长度动作序列。
- **多种规划策略**：允许在单一模型中实现粗到细、随机或固定粒度规划。

## 8. 不足与局限
- **计算资源未公开**：无法判断训练成本，影响可复现性。
- **规划策略需手动选择**：不同环境/偏移下最优策略不同，缺少自适应选择方法。
- **部分实验收益有限**：OGBench-Cube 上课程训练贡献不大，可能因为该任务本身难度与偏移无关。
- **环境覆盖有限**：仅含 2D 操纵、2D 导航、简单 3D 机械臂，未在真实机器人或高维视觉（如视频游戏）中验证。
- **未与层次世界模型（HWM）直接对比**：尽管文中提到 HWM 是相关工作，但实验未包含该基线。
- **潜在偏差**：数据来源于专家策略或启发式策略，可能无法泛化到次优数据。

（完）
