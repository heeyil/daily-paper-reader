---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长时程智能体强化学习的群图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 组图策略优化用于长程智能体强化学习
tldr: "长程智能体强化学习面临奖励稀疏和延迟问题，现有步级方法将轨迹视为孤立线性序列，忽略状态转移的图结构，导致估值高方差和短视信用分配。本文提出G2PO，将交互轨迹显式转化为全局状态转移图，通过组聚合状态价值估计降低采样偏差，并采用边中心优势估计标准化TD误差。在WebShop、ALFWorld、AppWorld等基准上，G2PO相比GRPO等基线最高提升22.2%成功率。该方法为长程智能体任务提供了细粒度、全局优化的新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 长程智能体RL中奖励稀疏且延迟，现有步级方法仍将探索视为线性轨迹，忽略状态转移图结构，导致高方差价值估计和局部信用分配。
method: 提出G2PO，将线性轨迹转化为全局状态转移图，通过组聚合状态价值估计降低方差，并基于边中心优势估计全局标准化TD误差以识别关键转移。
result: "在WebShop、ALFWorld、AppWorld上，G2PO成功率比GRPO等最优基线最高提升22.2%，显著优于SOTA提示和RL方法。"
conclusion: G2PO通过图结构建模和全局信用分配，有效解决了长程智能体RL中的稀疏延迟奖励问题，为多轮交互任务提供了更优策略优化框架。
---

## 摘要
基于群体的强化学习在智能体场景中显著提升了大型语言模型的能力。为实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步级训练。然而，长时程智能体强化学习面临严重的奖励稀疏和延迟问题，因为反馈通常延迟数十个交互步骤。现有步级框架虽细化了训练粒度，但其信用分配仍较为粗糙，且仍将智能体探索视为孤立的线性轨迹。这种过度简化的视角忽略了状态转移的固有图结构，导致状态值估计方差高、信用分配短视且局部化。为克服这些关键瓶颈，我们提出了群图策略优化（G2PO），一种专为多轮智能体任务设计的新型群体强化学习算法。G2PO明确将线性交互轨迹转化为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了群体聚合状态值估计，降低了采样方差和轨迹依赖偏差。此外，我们将智能体动作重新定义为状态节点间的转移，并提出了一种以边为中心的优势估计策略。通过在整个图上全局标准化时序差分误差，G2PO明确识别并优先处理驱动绝对任务进展的关键转移。在具有代表性的长时程基准测试——WebShop、ALFWorld和AppWorld上的大量实验表明，G2PO显著优于最先进的基于提示和强化学习的基线方法，成功率比GRPO提升了高达22.2%。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **背景**：大型语言模型（LLM）作为智能体在长时程（long-horizon）任务（如网页购物、具身操作）中面临奖励稀疏和延迟的严重挑战。现有的步级（step-level）强化学习（RL）框架虽然提高了训练粒度，但**仍将智能体探索视为一系列孤立的线性轨迹**，忽略了不同轨迹间可能遭遇相同或相似环境状态的事实。
*   **核心问题**：这种简化视角导致两个关键瓶颈：
    *   **状态价值估计高方差**：同一状态在不同轨迹中因后续随机性而获得不同终局奖励，单次采样估计不可靠。
    *   **信用分配的局部短视**：现有方法（如GiGPO）仅在局部同状态下比较动作，无法区分“微小改进”与“关键突破”的全局重要性。
*   **整体含义**：本文旨在通过显式建模状态转移的图结构，提供细粒度、全局化的信用分配，从而有效解决长时程RL中的稀疏延迟奖励问题。

### 2. 论文提出的方法论

*   **核心思想**：将采样得到的多条线性交互轨迹转化为**全局状态转移图**，在此基础上进行价值估计和优势计算。
*   **关键技术细节**：
    *   **状态组图构建**：将不同轨迹中相同/相似的观测（如相同网页内容）聚合成一个**状态组节点**（State Group Node），智能体的动作视为**边**，连接前后两个状态组节点，构建有向图 `{V, E}`。
    *   **组聚合状态价值估计**：
        *   先计算每条轨迹每个步骤的折扣回报 `v_ij = γ^(T-j+1) * R_i`（R_i为最终奖励）。
        *   然后定义状态组价值 `V(G_k)` 为该组内所有步骤 `v_ij` 的**均值**。这有效降低了单轨迹随机性或后续噪声导致的估值方差。
    *   **多粒度优势估计**：
        *   **节点中心优势 (Node-Centric, A_NC)**：在同一个状态组节点内，比较不同动作导致的下一个状态组价值，进行局部标准化。
        *   **边中心优势 (Edge-Centric, A_EC)**：用1步TD误差 `δ_ij = V(G_k') - V(G_k)` 衡量动作带来的绝对价值增量。然后将**所有轨迹、所有步骤的δ_ij收集成一个全局参考集**，进行全局标准化。这使得能显式识别并优先强化驱动绝对进展的关键转移。
        *   **回合级优势 (Episode-Level, A_EP)**：保留轨迹终局奖励的标准化，确保不偏离最终目标。
        *   **总优势**： `A_ij = A_EP + w * (A_NC + A_EC)`，其中w为平衡权重。
    *   **算法流程**：类似GRPO，但优势计算替换为上述多粒度优势。优化目标为裁剪后的策略梯度损失加上KL散度惩罚。

### 3. 实验设计

*   **数据集/场景**：三个具有代表性的长时程智能体基准：
    *   **WebShop**：电商网站场景。
    *   **ALFWorld**：具身家庭环境（包含Pick、Look等6个子任务）。
    *   **AppWorld**：复杂软件生态系统（通过Python API调用交互）。
*   **Benchmark**：各基准的标准任务集，评价指标为**成功率**（部分还包括分数）。
*   **对比方法**：
    *   **前沿离线模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。
    *   **提示方法**：Qwen2.5-Instruct、ReAct、Reflexion。
    *   **RL训练方法**：PPO（带Critic）、RLOO、GRPO、GiGPO（最新步级RL方法）。
*   **基础模型**：Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct（WebShop/ALFWorld）、Qwen2.5-14B-Instruct（AppWorld，因其难度较高）。

### 4. 资源与算力

*   **明确说明**：论文在附录E.3中给出了计算细节：
    *   **ALFWorld和WebShop**：使用 **4×H100 GPU**，训练 **100次迭代**（iteration）。
    *   **AppWorld**：使用 **8×H100 GPU**，训练 **50次迭代**。
    *   所有RL方法均使用128个环境并行采样（G2PO等组方法为16组×8环境）。
    *   **额外成本**：G2PO的优势计算阶段引入额外**约1秒**的CPU时间，占总训练时间约0.4%，几乎**无额外计算开销**。

### 5. 实验数量与充分性

*   **实验数量**：较为充足，涵盖：
    *   **主要基准**：在WebShop、ALFWorld（1.5B/7B）、AppWorld（14B）上报告了平均结果（3个随机种子）。
    *   **消融实验**：逐步添加A_NC、组聚合（GA）、A_EC，验证每个组件贡献（图3）。
    *   **超参数分析**：分析平衡因子w的影响（图4a），确定最佳值。
    *   **方差分析**：量化状态价值估计的方差分布，证明组聚合的必要性（图4b）。
    *   **效率分析**：比较不同方法的交互步数（图4c），显示G2PO更高效。
    *   **计算耗时分解**：展示各阶段时间（图5）。
    *   **组大小动态分析**：附录中展示训练过程中组大小的变化和分布（图6、7）。
    *   **案例研究**：提供具体图结构，说明边中心优势优于局部比较（图11）。
    *   **方差减少理论证明**：附录B提供数学证明。
*   **充分性/公平性**：实验设计较为充分客观。对比了3类基线（前沿模型、提示方法、多种RL方法），在多个规模模型上验证，并有消融和超参数分析。所有RL方法使用相同提示和超参数（PPO等除外），组大小一致。不过，仅在Qwen模型系列上实现，未扩展到其他LLM（如Llama、DeepSeek系列）；且仅基于verl-agent框架。

### 6. 论文的主要结论与发现

*   **显著性能提升**：G2PO在所有三个基准上**全面超越**所有提示和RL基线，包括最新的GiGPO。在WebShop上成功率比GRPO最高提升**22.2%**，在ALFWorld上提升**14.4%**。在AppWorld上也有提升（27.6% vs 24.8%）。
*   **组件有效性**：消融实验表明，组聚合价值估计和边中心优势都是性能提升的关键因素。
*   **训练效率高**：G2PO不仅提升性能，还**减少**了完成任务所需的交互步数（更高效），且几乎不增加计算开销。
*   **理论支持**：提供了方差减少的数学证明，验证了方法的合理性。

### 7. 优点

*   **方法创新性强**：首次将交互轨迹显式建模为全局状态转移图，从根本上克服了线性视角的局限性。
*   **信用分配精细且全局**：边中心优势通过全局标准化TD误差，解决了局部比较无法区分动作绝对重要性的问题。
*   **实用高效**：几乎无额外计算成本（1秒CPU开销），且能减少交互步数，成本效益高。
*   **实验严谨完备**：覆盖多个任务、多种模型规模，包含消融、超参数分析、理论证明和案例，论证充分。
*   **鲁棒性强**：在观测形式差异大的复杂场景（AppWorld的API返回）下仍有效。

### 8. 不足与局限

*   **框架与模型泛化**：论文仅基于verl-agent框架实现，且仅在Qwen模型上实验，未验证在更多RL框架（如RAGEN、LOOP）和不同LLM（如Llama、Mistral）上的泛化性。
*   **状态匹配依赖**：构建状态组图依赖观测的精确匹配（或非常相似），对于语义等价但文本不同的观测可能无法正确聚合，影响效果。
*   **采样依赖性**：组聚合和全局标准化的效果依赖于采样轨迹的多样性。如果采样数量少或多样性不足，组大小可能很小（如大小为1），估值方差降低有限。
*   **任务适用性**：在AppWorld上提升幅度相对较小（约3个百分点），可能因为该任务中状态图结构较复杂或信用分配本身较难。
*   **未讨论复杂图循环**：论文假设图结构中状态转移是向前的，未涉及循环或重复状态的长期依赖处理。

（完）
