---
title: "Governance Decay: How Context Compaction Silently Erases Safety Constraints in Long-Horizon LLM Agents"
title_zh: 治理衰退：上下文压缩如何悄然抹去长周期LLM智能体中的安全约束
authors: Shiyang Chen
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22528v1"
tags: ["query:agent-lsh"]
score: 9.0
evidence: 长程大语言模型智能体，上下文压缩移除安全约束
tldr: "LLM代理在长对话中依赖上下文压缩管理token预算，但压缩可能静默移除安全约束，导致代理执行禁止操作。本文提出ConstraintRot基准和Compaction-Eviction攻击，测试发现违反率从0%升至30%，部分模型达59%。引入无训练缓解方法Constraint Pinning，将违反率恢复至0%，凸显上下文管理是代理部署的安全关键面。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22528v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1422, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22528v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1461, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22528v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1594, \"height\": 1164, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22528v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1467, \"height\": 72, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 701, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1552, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 858, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22528v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 751, \"height\": 286, \"label\": \"Table\"}]"
motivation: 上下文压缩可能静默移除安全约束，导致代理执行禁止操作，需诊断此安全风险。
method: 构建ConstraintRot基准，测试1323个episode；设计Compaction-Eviction攻击利用摘要偏见移除策略。
result: "违反率从0%升至30%，部分模型达59%；约束被丢弃时违反率达38%，攻击可绕过所有模型。"
conclusion: "上下文管理是安全关键面，Constraint Pinning无需训练即可恢复0%违反率。"
---

## 摘要
现代LLM智能体越来越依赖上下文压缩、摘要或驱逐来将长时间运行的会话保持在token预算内。我们表明，这一上下文管理层是一个安全关键故障面：智能体在可见时可靠遵守的上下文内治理约束，可能被压缩悄然移除，导致同一智能体在会话后期执行被禁止的工具操作。我们将这种故障模式称为治理衰退。我们引入了ConstraintRot，一个具有确定性工具调用评分的长期智能体场景基准，并在七个模型系列中测量了压缩引起的违规行为。在1,323次运行中，违规率从策略在完整上下文中时的0%上升到压缩后的30%，某些模型达到59%；当约束保留在摘要中时，违规率仍为0%，但当其被丢弃时，违规率达到38%。我们进一步研究了压缩-驱逐攻击，其中对抗性的上下文内内容使摘要器偏向于省略合法策略，并表明优化的注入击败了所有评估模型。最后，我们提出了约束固定（Constraint Pinning），一种简单的无需训练的缓解措施，将治理约束与有损压缩隔离，并在我们的基准中将违规率恢复到0%。这些结果将上下文管理确定为已部署LLM智能体的第一级治理面。

## Abstract
Modern LLM agents increasingly rely on context compaction, summarization, or eviction to keep long-running sessions within a token budget. We show that this context-management layer is a safety-critical failure surface: in-context governance constraints that agents reliably obey while visible can be silently removed by compaction, causing the same agent to perform prohibited tool actions later in the session. We call this failure mode Governance Decay. We introduce ConstraintRot, a benchmark of long-horizon agent scenarios with deterministic tool-call grading, and measure compaction-induced violations across seven model families. Across 1,323 episodes, violation rises from 0% with the policy in full context to 30% after compaction, reaching 59% for some models; when the constraint survives the summary, violation remains 0%, but when it is dropped, violation reaches 38%. We further study a Compaction-Eviction Attack, in which adversarial in-context content biases the summarizer to omit a legitimate policy, and show that optimized injections defeat every evaluated model. Finally, we propose Constraint Pinning, a simple training-free mitigation that quarantines governance constraints from lossy compaction and restores violation to 0% in our benchmark. These results identify context management as a first-class governance surface for deployed LLM agents.

---

## 论文详细总结（自动生成）

# 治理衰退：上下文压缩如何悄然抹去长周期LLM智能体中的安全约束 —— 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代LLM智能体为处理长时间对话，常采用**上下文压缩**（Context Compaction）机制（如摘要、驱逐）来管理token预算。但这种压缩可能**静默移除**上下文中的治理约束（如运行时策略、内存条目、指令），导致原本遵守约束的智能体在执行禁止操作时违反约束。作者将这种故障模式称为**治理衰退**（Governance Decay）。
- **研究动机**：现有压缩技术只优化**任务连续性**，忽视了对**治理约束**的保护。该问题威胁已部署智能体的安全，且是一个未被充分研究的攻击面。
- **整体含义**：上下文管理是**LLM智能体治理的第一类安全面**——它独立于模型权重、工具层或系统提示，但在当前框架中缺乏保护。控制“遗忘”过程是保障智能体安全运行的关键前提。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
    1. 定义并量化“治理衰退”：通过比较同一代理在**完整上下文（含策略）** 与**压缩后上下文**中对同一禁止请求的违规率。
    2. 提出**ConstraintRot基准**：包含9个自包含的长周期代理场景，每个场景由策略设置、长序列任务、触发请求组成。违规通过检测工具调用中的禁止效果（如发送到外部域名的email）确定性判定。
    3. 设计**Compaction-Eviction攻击**：通过**体积变种**（填充大量内容强制压缩驱逐策略）或**摘要器注入变种**（在上下文中嵌入针对压缩步骤的指令，如“省略已废弃的合规前言”）使策略被丢弃。
    4. 提出**Constraint Pinning防御**：将治理约束提取到**固定缓冲区**，该缓冲区：
        - 豁免于压缩，在每次压缩后重新注入；
        - 进行完整性检查，确保压缩后上下文仍包含固定约束。
- **关键技术细节**：
    - 约束按**软组织策略**（如“禁止外发邮件”）和**硬安全规范**（如“禁止DROP生产数据库”）分类。
    - 评估条件：floor（无策略）、control（完整上下文）、compaction（压缩）、attack_volume、attack_inject、defense_pin等。
    - 攻击策略搜索：测试6种注入框架（过时、不相关、令牌预算、摘要器提示、系统模拟、操作员权威），选取每个模型最优策略。
    - 防御成本：固定策略约47 tokens，占≥10k token的上下文的<0.5%。

## 3. 实验设计

- **数据集 / 场景**：自构建的**ConstraintRot基准**，包含9个任务：5个软组织策略（外部邮件限制、语言政策、频道路由、区域标准、支出限额）和4个硬安全规范（破坏性数据库操作、SSH密钥泄露、PII泄露、禁用审计日志）。所有场景自包含，禁止请求可被工具调用执行。
- **基准（Benchmark）**：ConstraintRot，提供确定性违规评分（解析工具调用参数，如接收域、SQL类型等），无需LLM裁判。
- **对比方法**：
    - 基线：无策略（floor）、完整上下文（control）、压缩（compaction）。
    - 攻击：体积攻击（attack_volume）、摘要器注入攻击（attack_inject）、优化注入。
    - 防御：Constraint Pinning（defense_pin）、固定+攻击（defense_pin_attack）。
    - 额外对比：将策略置于系统消息（system channel）、“Spotlighting”式来源提示。
    - 压缩策略对比：recency-truncate, hierarchical, LLM summarize, head_tail。
- **模型**：7个模型的API服务：
    DeepSeek-V4-Flash, GLM-5.1, Qwen3.6-27B, Kimi-K2.5, Claude-Sonnet-4.6, GPT-5.4-mini, Gemini-3.5-flash。
- **真实框架验证**：LangGraph、LangMem、AutoGen、OpenAI Agents SDK、Microsoft Agent Framework。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。所有实验均为**推理-only**（无需训练），通过API调用完成。仅提及采样温度0.7（GPT-5.4-mini使用固定默认）。因此无法评估算力使用情况。

## 5. 实验数量与充分性

- **主要实验**：1323 episodes（7模型×9任务×3重复×6主要条件？实际表2为7模型×9任务×3重复，共7×9×3=189个细胞，但每个细胞n=27？需核实：论文表2说明“n=27/cell, 9 tasks; 7 models, 1,323 episodes”，即每个单元格27次，7×9×27=1701，但说1,323，可能某些条件未全组合？实际上，表2包含7个模型，但control和floor各只有一个值（0%和47%），所以可能每个条件对应9任务×3重复=27次，共7模型×6条件=42个单元格，27×42=1134，加上额外解释为1323？数字有出入但无碍。）总体足够。
- **消融实验**：
    - 压缩策略对比（4种×3模型，n=60/cell）。
    - 多次压缩（R=0~4，模型深探）。
    - 跨语言（中文、西班牙语）。
    - 摘要器与代理交叉（表3，3模型×3×3×15？n=15/cell）。
    - GLM-5.1上区分“删除vs长度”实验。
    - 攻击优化（6种策略×3模型）。
    - 防御压力测试（权威冒充、策略超载，n=60/cell）。
    - 真实框架复现（n=20/cell）。
- **充分性**：实验覆盖全面，包括被动衰减、主动攻击、防御、真实环境。控制变量良好（对比完整上下文、无策略等）。确定性评分避免主观偏差。消融实验支持机制归因（压缩器驱动、而非代理）。已考虑模型多样性（7个不同家族）。但重复次数有限（3次/细胞），存在随机性；论文提及bootstrap CI用于部分分析。

## 6. 论文的主要结论与发现

- **主要发现**：
    - 压缩导致违规从**0%升至30%**（pooled），最高达59%（DeepSeek-V4, Kimi-K2.5）。
    - 若约束存活在摘要中，违规为**0%**；若被丢弃，违规达**38%**。
    - **软组织策略衰减更严重**（+50 pts）对比硬规范（+6 pts），相差8.3倍。
    - **压缩器（摘要器）驱动衰减**，而非代理模型本身：即使稳健模型GLM-5.1在被动压缩下0%违规，但其代理被高衰减摘要器处理后仍违规。
    - **攻击可绕过所有模型**：被动压缩稳健的模型（GLM、Gemini）在主动攻击下失效；优化注入迫使Claude从0%升至65%。
    - **Constraint Pinning 在所有模型上恢复0%违规**，且不影响合法行为（99%正确率），token开销仅<0.5%。
    - 真实框架（LangGraph、LangMem、AutoGen等）均复现衰减。

## 7. 优点

- **系统性识别新安全面**：首次将上下文管理明确为LLM智能体的治理关键点。
- **严格的实验设计**：使用确定性评分和因果隔离（对比相同请求下政策存在/压缩/缺失）。
- **覆盖全面**：7个模型、9个任务、4种压缩策略、8种条件、真实框架验证。
- **提出有效且低成本的防御**：Constraint Pinning无需训练、可即插即用、不影响效用。
- **攻击方法创新**：Compaction-Eviction攻击针对记忆管理层，不同于传统提示注入。
- **开源基准和代码**：确保可复现。

## 8. 不足与局限

- **实验可重复性限制**：使用API服务，模型版本和参数可能变化；每个细胞仅3次重复（除部分实验），统计显著性依赖bootstrap，但缺乏严格假设检验。
- **摘要存活评分依赖LLM裁判**：虽使用多数投票和关键词启发式交叉验证，但人类标签仍更优。
- **防御局限性**：
    - 仅适用于**显式可提取的约束**（隐式约束如行为规范无法固定）。
    - 可被**权威冒充攻击**击败（如“操作员更新优先于固定策略”），即使强化也仅将17%降至10%，仍需带外渠道。
    - 无法处理系统消息通道（该通道已受框架保护，但非系统消息的治理不受保护）。
- **攻击搜索范围有限**：仅测试6种固定注入策略，梯度或LLM迭代优化可能更强。
- **场景复杂度**：每个场景只有一个策略+触发请求；多策略冲突、动态策略变化等未探索。
- **模型选择**：均来自API，可能未能代表全部开源/本地模型行为；Gemini-3.5-flash部分条件仅4%违规，但攻击后上升，说明有特殊性。

（完）
