---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连点成线：通过强化学习训练大语言模型以具备跨领域泛化的长期生命周期智能体能力
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:agent-lsh"]
score: 10.0
evidence: 通过强化学习训练长期生命周期自改进代理
tldr: 长期生命周期代理需要大语言模型（LLM）具备“连接点”（CoD）元能力，即在环境中持续探索、学习并更新上下文以提升后续任务表现。本文提出CoD框架，通过端到端强化学习（RL）训练，交替执行求解任务和更新上下文的轨迹，并采用带细粒度信用分配的GRPO-style算法。实验验证了RL训练的有效性，并展示了在同一领域、跨领域以及从CoD到Ralph-loop设置中的分布外泛化能力。该工作连接了多项先前研究，为LLM和AI代理的进步开辟了新机会。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 训练LLM具备长期生命周期代理所需的跨领域泛化元能力，使其能持续探索环境、从经验中学习并自我更新上下文。
method: 提出CoD框架，包含端到端RL算法（GRPO-style）与细粒度信用分配，以及为激励和评估元能力设计的任务与环境。
result: 实验表明CoD框架的RL训练有效，且引发的元能力在训练域内、跨域及到Ralph-loop设置中均展现出分布外泛化潜力。
conclusion: CoD框架将多行先验工作连接起来，为推进LLM和AI代理提供了新的研究方向和应用机遇。
---

## 摘要
本工作提出了一个通用框架，用于训练大语言模型（LLMs）具备“连点成线”（CoD）这一元能力，该能力是长期生命周期智能体所必需的：当基于LLM的AI智能体部署在环境中时，它会解决一系列长期任务，同时持续探索环境、从自身经验中学习，并迭代地自我更新关于环境的上下文，从而在更新上下文的基础上，在未来任务中逐步取得更优的性能。CoD框架的主要组成部分包括：（1）用于端到端强化学习（RL）的算法设计和基础设施，其中包含交替进行任务解决和上下文更新情节的长程展开序列；（2）用于在训练过程中激励和激发LLMs中目标元能力的任务和环境，以及在评估过程中忠实衡量进展的任务和环境。我们展示了CoD框架的概念验证实现，包括采用细粒度信用分配的GRPO风格RL算法，以及针对目标元能力（而非特定领域LLM能力或标准逐任务RL）定制的任务和环境。实证结果验证了在CoD设置中进行端到端RL训练的有效性，并展示了所激发元能力在分布外泛化（包括训练域内、跨不同域，以及从CoD到Ralph-loop设置）的潜力。我们对CoD的研究连接了先前多个工作方向，并为推进LLMs和AI智能体开辟了新的机会。为促进进一步研究和应用，我们在\url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}上发布了我们的实现。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

# 论文《Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning》详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：当前大语言模型（LLM）作为 AI 智能体部署时，往往缺乏长期生命周期能力。智能体需要在同一环境中连续解决一系列不同但相关的任务，同时持续探索环境、从自身经验学习，并自我更新关于环境的上下文（例如提示、技能记忆等），从而在后续任务中逐步提升表现。这种“连点成线”（Connect the Dots，CoD）的元能力被认为是长期部署的关键。
- **背景现状**：已有研究指出，LLM 在多轮对话或未指定环境中容易迷失方向，且依赖人工设计的复杂智能体框架才能稳定运行。标准逐任务 RL 训练（每次任务从零开始）与 CoD 所需的能力不匹配。
- **整体含义**：论文提出一个通用框架 CoD，通过端到端强化学习（RL）显式训练 LLM 获得这种元能力，并验证其在训练域内、跨域及 Ralph-loop 设置中的泛化潜力。

## 2. 方法论

- **核心思想**：将长期部署抽象为“CoD-Deploy”，交替执行“求解任务”情节和“更新上下文”情节。在训练时，采用与部署一致的展开模式，即“CoD-Train”，通过端到端 RL 优化模型参数。
- **关键技术细节**：
  - **RL 算法**：采用 GRPO-style 算法，无批评模型，假设同一任务序列有多个展开轨迹。引入**细粒度信用分配**：将每个情节的回报定义为当前及未来求解任务奖励的平均值；然后对同一位置的各情节取平均作为基线，计算每个情节的优势。
  - **梯度计算**：结合 REC-OneSide-NoIS（去除重要性采样权重）和自适应重加权（RED-Weight），以缓解平均优势衰减导致的训练不稳定（见表 3 比较）。
  - **上下文机制**：当前实现采用单一文本“提示（hint）”，模型在更新上下文情节中生成新提示，替换旧提示。
- **任务与环境设计**：
  - **FrozenLake-Obscure**：经典 FrozenLake 变体，动作空间为 A/B/C/D，与上下左右的映射随机且未知，迫使智能体通过探索推断映射并跨任务传递。
  - **Alchemy-Random**：随机初始化元素和合成配方，智能体需在有限步骤内合成目标元素，鼓励发现和记录有效配方及失败组合。
  - **TerminalSimulator**：模拟终端环境，测试文件操作等日常任务，环境特性（如操作系统）可变。

## 3. 实验设计

- **初始模型**：Qwen3-8B-Instruct（关闭思考模式）。
- **两个主要训练设置**：
  - **Setting A**：在 FrozenLake-Obscure 域上 CoD-Train，任务序列长度 4。
  - **Setting B**：在混合域（FrozenLake-Obscure + Alchemy-Random）上 CoD-Train，序列长度 4。
- **评估场景**：
  - **域内 OOD 评估**：使用更难的实例（更大地图、更多元素）和更长序列（长度 8）。
  - **跨域 OOD 评估**：在未见的域（TerminalSimulator）上测试。
  - **Ralph-loop 设置**：重复求解同一任务，视为 CoD-Deploy 的特例。
- **对比方法**：仅对 RL 算法本身进行比较（标准 GRPO、REC-OneSide-NoIS、最终算法），未对比其他模型或基线框架。
- **数据量**：每个环境有 50,000 训练实例和 4,000 测试实例。

## 4. 资源与算力

- **论文未明确说明**：文中未提及使用的 GPU 型号、数量、训练时长等算力细节。仅在附录表 1 列出超参数（如学习率 1e-6、rollout batch size 32、group size 8、训练 1 epoch 等）。

## 5. 实验数量与充分性

- **数量**：进行了两大主实验（Setting A 和 B），在每个设置中绘制了训练奖励曲线和多个 OOD 评估曲线（域内、跨域、Ralph-loop）。还额外比较了三种 RL 算法变体。
- **充分性**：
  - 实验设计较为全面，涵盖了域内、跨域和特殊设置的泛化测试，并关注了训练稳定性。
  - 但仅使用了 8B 参数模型，未在更大规模或不同家族模型上验证。
  - 无消融实验分离上下文更新机制（如固定上下文 vs 自更新上下文）对性能增益的贡献。
  - 跨域泛化到 TerminalSimulator 的提升被作者谨慎解释，可能不完全归因于 CoD 元能力。

## 6. 主要结论与发现

- **训练有效性**：在 FrozenLake-Obscure 上，位置 0 的平均奖励从约 0.18 提升至 0.45（信息理论极限），而位置 3 的奖励从 0.28 大幅提升至 0.76，表明模型学会了利用自更新上下文。
- **泛化能力**：在域内（更难实例、更长序列）、跨域（Alchemy-Random、TerminalSimulator）以及 Ralph-loop 设置中均观察到奖励提升，提示 CoD 元能力具备跨域泛化潜力。
- **算法稳定性**：最终的自适应重加权方法优于标准 GRPO 和 REC-OneSide-NoIS，特别是在 Alchemy-Random 域上。

## 7. 优点

- **问题新颖**：正式定义了“长期生命周期智能体”所需的 CoD 元能力，并为此设计了专门框架。
- **方法论创新**：将经典动态规划原理应用于 LLM RL 的信用分配，实现了细粒度回报估计；GRPO-style 算法无需批评模型，降低训练成本。
- **环境设计针对性**：FrozenLake-Obscure 和 Alchemy-Random 明确激励上下文跨任务转移，避免了标准基准（如数学推理）的干扰。
- **泛化验证全面**：涵盖了域内、跨域和特殊设置（Ralph-loop），展示了元能力的转移潜力。

## 8. 不足与局限

- **算力资源未透明**：缺少对计算成本的报告，不利于复现和公平比较。
- **模型规模有限**：仅使用 Qwen3-8B，未在更大模型（如 32B/72B）上验证，泛化结论可能受限。
- **环境设计简单**：FrozenLake-Obscure 和 Alchemy-Random 仍属游戏化场景，与真实世界应用差距较大；TerminalSimulator 任务间关联性弱，跨域泛化机制需进一步分析。
- **上下文机制简化**：仅使用单一文本“提示”，未探索更复杂的记忆库或技能文件形式，可能限制了能力上限。
- **算法仍有启发式成分**：自适应重加权虽稳定但缺乏理论保证；信用分配未考虑折扣或有限窗口，在极长序列中可能受限。
- **缺乏消融实验**：未分离出“上下文更新”与“标准多回合 RL”的贡献，也未分析训练数据比例等影响。

（完）
