---
title: "From Sampled Outcomes to Capability Distributions: Rethinking Supervision for LLM Routing"
title_zh: 从采样结果到能力分布：重新思考大语言模型路由的监督信号
authors: "Guannan Lai, Haoran Hu, Long Chen, Zhenguo Li, Han-Jia Ye"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.06924v1"
tags: ["query:moe-routing"]
score: 9.0
evidence: LLM路由监督、分布感知
tldr: 现有LLM路由方法通常使用模型单次响应作为能力标签，但由于生成随机性，这种单次监督只提供噪声观测，导致路由策略不可靠。为此，提出DARS（分布感知路由监督）框架，从输入和输出两侧考虑不确定性，构建分布级的监督信号。实验表明，单次标签会误导模型选择，而分布感知监督提供更稳定的标签并改善路由行为。这项工作表明可靠的LLM路由应基于查询级能力分布而非单次响应。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06924v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 721, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06924v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1649, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06924v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1316, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06924v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1649, \"height\": 450, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06924v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 797, \"height\": 743, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06924v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 802, \"height\": 377, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06924v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1639, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06924v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1220, \"height\": 1048, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06924v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1649, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06924v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1646, \"height\": 286, \"label\": \"Table\"}]"
motivation: 单次生成响应作为路由监督引入系统噪声，使学习到的路由策略不可靠。
method: DARS框架从输入和输出两侧考虑不确定性，基于分布级观察构建更可靠的路由监督信号。
result: 分布感知监督提供更稳定标签，改善了学习到的路由行为，优于单次标签。
conclusion: 可靠LLM路由应超越单次响应观测，基于查询级模型能力分布。
---

## 摘要
现有的大语言模型路由方法通常将模型对查询的单次响应作为训练路由器的能力标签。然而，由于大语言模型生成本质上具有随机性，这种单次监督仅提供了查询-模型对行为的噪声观测，而非可靠的能力估计。我们表明，这一假设会在路由监督中引入系统性噪声，使得学到的路由策略可靠性降低。为了解决这一问题，我们提出DARS（分布感知路由监督）框架，该框架从模型行为的分布视角构建路由监督。DARS不依赖于单一生成响应，而是同时考虑输入侧和输出侧的不确定性，捕捉语义等价的查询表述和随机生成如何影响模型性能。基于这些分布感知的观测，DARS为路由构建更可靠的监督信号。跨多种任务的实验表明，单次标签可能误导模型选择，而分布感知监督提供了更稳定的标签并改进了学习到的路由行为。我们的结果表明，可靠的大语言模型路由应超越单次响应观测，并基于查询级别的模型能力分布。

## Abstract
Existing LLM routing methods typically treat a model's single response to a query as its capability label for training routers. However, because LLM generation is inherently stochastic, such single-shot supervision provides only a noisy observation of a query-model pair's behavior rather than a reliable capability estimate. We show that this assumption introduces systematic noise into routing supervision, making learned routing policies less reliable. To address this issue, we propose DARS (Distribution-Aware Routing Supervision), a framework that constructs routing supervision from a distributional view of model behavior. Instead of relying on a single generated response, DARS considers uncertainty from both the input side and the output side, capturing how semantically equivalent query formulations and stochastic generations affect model performance. Based on these distribution-aware observations, DARS builds more reliable supervision signals for routing. Experiments across diverse tasks show that single-shot labels can be misleading for model selection, while distribution-aware supervision provides more stable labels and improves learned routing behavior. Our results suggest that reliable LLM routing should move beyond single-response observations and be grounded in query-level model capability distributions.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究动机**  
现有大语言模型（LLM）路由方法普遍将模型对查询的**单次生成响应**作为其能力标签，用于训练路由器（router）。然而，LLM生成具有内在随机性，不同采样结果可能对应完全不同的模型偏好。这种“单次监督”本质上是查询-模型对行为的噪声观测，而非可靠的能力估计。论文系统地指出：单次标签的不稳定性会**级联到路由决策**，使得学到的路由策略反映的是采样噪声而非模型之间的稳定能力差异。

**整体含义**  
论文呼吁将路由监督从“点估计”转向**分布视角**，即通过重复观测（输入侧重写 + 输出侧多次解码）来估计每个查询-模型对的**能力分布**，并构建风险感知的路由信号。这项工作重新定义了LLM路由的监督基础，对路由系统的可靠性和鲁棒性具有根本性意义。

## 2. 论文提出的方法论

### 核心思想
DARS（Distribution-Aware Routing Supervision）框架用**分布级能力信号**替代单次观测，同时考虑两种不确定性：
- **输入侧不确定性**：语义等价的查询表述（通过提示重写引入）
- **输出侧不确定性**：同一查询下随机解码的变异性

### 关键技术细节
1. **重复观测构建**：对每个训练查询，用强LLM（GPT-4o）生成N个语义保留的重写，每个重写后对每个模型进行M次随机解码，得到N×M个观测。
2. **分布能力估计**：
   - 期望质量 \( \mu_q(x,m) = \text{Mean}(\{q^{(n,j)}_m\}) \)
   - 期望成本 \( \mu_c(x,m) = \text{Mean}(\{c^{(n,j)}_m\}) \)
   - 性能风险 \( \sigma_q(x,m) = \text{Std}(\{q^{(n,j)}_m\}) \)
3. **风险感知效用**：
   \[
   U(x,m) = \mu_q(x,m) - \lambda \mu_c(x,m) - \beta \sigma_q(x,m)
   \]
   其中λ控制成本-质量权衡，β控制风险惩罚。
4. **路由策略学习**：DARS提供多种适配方式，包括回归目标（预测μ_q, μ_c, σ_q）、效用直接监督、分类软标签（Softmax over utilities）、排序偏好等。

### 算法流程
- 输入：训练查询集、模型池M、重写数N、解码次数M
- 步骤1：对每个查询生成N个重写
- 步骤2：对每个重写和每个模型进行M次随机解码，记录质量和成本
- 步骤3：计算每个查询-模型对的μ_q, μ_c, σ_q
- 步骤4：构造风险感知效用U(x,m)，确定最佳标签或连续目标
- 步骤5：用这些监督信号训练任意路由器（基于给定超参数λ, β）
- 推理时路由器预测U并选择模型argmax

## 3. 实验设计

### 数据集
- **GPQA**：研究生级别多项选择科学题（生物/物理/化学）
- **MATH-500**：MATH子集，500道数学解题题
- **DROP-800**：DROP子集，800道阅读理解题（需计数、排序、算术推理）

三个数据集覆盖不同输出格式（多选、自由形式、段落推理），考察不同不确定性来源。

### Benchmark
- 未使用单一标准benchmark，而是自建路由测试协议：每个测试查询评估两个分数
  - **U_rew**(重写基础效用)：用3个语义重写评估所选模型
  - **U_dec**(解码基础效用)：用3次随机解码评估所选模型

### 对比方法
- **回归类**：MLPRouter、MIRT（项目反应理论）、EmbedLLM
- **分类类**：RM-Softmax、GraphRouter
- **聚类类**：AvengersPro
- **非参数检索类**：kNNRouter
- 所有方法在**相同路由器架构**下对比单次监督 vs DARS增强，保证公平。

### 模型池
6个异构LLM：Gemma-3-12B-IT、Mistral-Small-3.2-24B-Instruct、Qwen3-32B、Llama-3.3-70B-Instruct、Gemini-2.5-Flash-Lite、DeepSeek-Chat-V3.1（涵盖开源/API模型、不同大小和部署成本）。

## 4. 资源与算力

**论文未明确说明使用的GPU型号、数量或训练时长**。仅在实验设置中提及使用GPT-4o生成查询重写，以及各模型通过公开API或本地接口运行。因此无法量化具体算力消耗。

## 5. 实验数量与充分性

### 实验组数
1. **诊断分析（图2）**：3个数据集上报告3类指标（结果不稳定性、胜者翻转率、单次vs分布一致性），并分解输入/输出不确定性，以及单次监督导致的路由方差（100次重复训练）。
2. **主要对比（表1）**：7种路由器 × 3个数据集 × 2种测试视图 = 42组对比，单次监督结果为100次重复平均。
3. **消融研究（表2）**：7种变体（单次、仅输入、仅输出、无风险、仅重写风险、仅解码风险、完整DARS），在3个数据集上2种视图，共42组对比。
4. **样本效率（图4a）**：不同观测协议（1×1到5×5）在3个数据集上的效用增益。
5. **成本-质量权衡（图4b）**：不同成本约束下7种路由器的Pareto前沿对比。
6. **风险系数敏感性（图4c）**：β在[0,0.5]范围内变化对性能的影响。

### 充分性评价
- ✅ **多方面覆盖**：涵盖不同任务类型、不同路由器家族、多个超参数（λ、β）
- ✅ **统计稳健**：单次监督结果基于100次独立采样取平均值，降低了随机性影响
- ✅ **消融彻底**：分别分离输入/输出风险成分、风险项贡献
- ✅ **现实性验证**：附录D提供重写有效性检查和评分有效性检查，确保预处理质量
- **潜在不足**：所有实验基于6个模型的中等规模池，未在更大规模（如数十个模型）上验证；仅使用三种数据集，任务多样性有限（缺少对话、生成等开放任务）

## 6. 论文的主要结论与发现

1. **单次标签具有系统性不稳定性**：在GPQA上胜者翻转率高达0.970，即使较稳定的MATH-500也达0.645；单次与分布标签的歧异率在DROP-800上达0.471。
2. **不同任务具有不同不确定性画像**：GPQA和MATH-500以输出侧不确定性为主，DROP-800输入、输出均显著。
3. **单次监督导致路由策略不稳定**：100个不同单次样本训练的路由器性能标准差可达0.018（GPQA），说明路由策略对采样噪声敏感。
4. **DARS显著提升各类路由器性能**：在所有7种路由器、3个数据集上，DARS增强版几乎都优于单次版本；特别在GPQA上提升最大。
5. **3×3观测即可接近5×5饱和性能**：DARS样本效率高，实际部署成本可控。
6. **风险项贡献明确**：包含性能变异性的完整DARS优于仅使用均值（无风险）的版本，输入和输出风险互补。
7. **DARS改进Pareto前沿**：在不同成本约束下均能提供更优的成本-质量折衷，甚至可以超越最佳固定模型。

## 7. 优点

- **问题洞察深刻**：首次系统指出现有路由监督中“单次假设”的根本缺陷，并从理论上识别输入/输出两种不确定性。
- **方法通用灵活**：DARS仅改造监督信号，不绑定特定路由器架构，可无缝集成到回归、分类、排序、检索等各类路由器中。
- **实验设计严谨公平**：所有对比保持路由器架构不变，仅替换监督；单次结果经100次重复，统计可靠。
- **分析全面**：不仅报告最终性能，还诊断标签层面的不稳定性（胜者翻转、不确定性分解、路由方差），提供深入理解。
- **实用性考量**：样本效率分析表明少量重复即可达到良好效果，降低部署成本；风险系数敏感性分析显示β在宽范围内表现稳定。

## 8. 不足与局限

- **数据集规模与多样性有限**：仅使用200训练样本（GPQA）且任务类型以选择题和数学/阅读理解为主，未涵盖开放式对话、长文本生成、代码生成等更广泛的应用场景。
- **模型池规模有限**：仅含6个模型，未验证在更大规模（如数十个）模型池上的可扩展性和收敛性；不同模型间的能力差异可能不够极端。
- **重写依赖强LLM**：查询重写使用GPT-4o，若换成弱LLM可能引入偏差或质量下降，论文未讨论这种依赖性。
- **未考虑推理时实时性**：DARS训练阶段需要多次采样，但推理时路由器仍需快速决策；论文未分析DARS训练开销对部署的影响（如多次采样带来的额外API成本）。
- **缺乏正式理论保证**：论文未提供关于分布估计误差界或者风险效用最优性的理论分析。
- **评分自动化潜在风险**：自动评分（特别是开放式数学题）可能存在解析错误，论文虽在附录D给出了检查，但仍无法完全避免。
- **未讨论对比方法的超参数是否公平调优**：每个路由器可能有多类超参数（如MLP网络结构、MIRT维度等），论文未说明与其基线论文保持一致还是统一设定，存在调优偏差风险。

（完）
